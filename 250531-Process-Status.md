# 프로세스의 주요 상태 status

커널은 프로세스를 효율적으로 관리하기 위해 여러 상태를 정의하고, 상황에 따라 프로세스의 상태를 계속 바꾼다. 이 상태 정보는 PCB에 기록된다. 주요 상태는 다음과 같다.

- **Running**: CPU가 해당 프로세스의 코드를 실제로 실행하고 있는 상태. 엄밀히 말해 특정 순간에 하나의 CPU 코어에서는 단 하나의 프로세스만 실행 상태일 수 있다. `ps` 명령어에서는 `R`로 표시된다.

- **Ready**: 실행할 준비가 모두 끝났는데, 다른 프로세스가 CPU를 쓰고 있어서 순서를 기다리는 상태. 스케줄러가 CPU를 할당해주기만 하면 언제든 실행될 수 있다.

- **대기 Waiting / Sleeping**: 프로세스가 특정 이벤트가 발생하기를 기다리며 잠들어 있는 상태. 예를 들어 디스크 i/o가 끝나기를 기다리거나, 네트워크 응답을 기다리거나, 특정 시간(sleep)이 되기를 기다리는 경우가 해당된다. 이 상태의 프로세스는 CPU를 주어도 당장 실행될 수 없다. 기다리는 이벤트가 완료되면 커널에 의해 'Ready' 상태로 바뀐다. `ps` 명령어에서는 `S`(interruptible sleep) 또는 `D`(uninterruptible sleep)로 표시된다.

이 상태들은 계속해서 순환한다. **Ready** 상태의 프로세스가 스케줄러에 의해 선택되면 **Running** 상태가 되고, 실행되던 프로세스가 입출력을 요청하면 **Ready** 상태로 바뀐다. 입출력이 끝나면 다시 **Ready** 상태로 돌아가 CPU를 기다리는 식이다.

### 좀비와 고아 프로세스

- **좀비 프로세스 (Zombie)**: 프로세스가 모든 실행을 마치고 종료되었는데, 아직 부모 프로세스가 그 자식프로세스를 회수해가지 않은 상태. 프로세스가 일을 다 끝내고 커널에 자원을 모두 반납했지만, PCB 정보는 부모가 확인할 수 있도록 프로세스 테이블에 남아있는 거다. 이 상태의 프로세스는 이미 죽어있기 때문에 CPU를 사용하지는 않지만, PID와 프로세스 테이블의 한 자리를 계속 차지한다. 부모 프로세스는 `wait()` 시스템 콜을 호출해서 자식의 종료 상태를 확인하고, 이때 비로소 좀비 프로세스는 완전히 소멸한다. `ps` 명령어에서는 `Z`로 표시된다.

- **고아 프로세스 (Orphan)**: 부모 프로세스가 자식 프로세스보다 먼저 종료된 경우, 남겨진 자식 프로세스를 '고아 프로세스'라고 한다. 이 고아들을 방치하면 나중에 이들이 종료되었을 때 종료 상태를 거두어 갈 부모가 없으므로 좀비로 남게 된다. 리눅스 커널은 이런 상황을 막기 위해, 고아 프로세스가 생기면 즉시 모든 프로세스의 조상인 **`init` 프로세스(PID 1)를 새로운 부모로 입양**시킨다. `init` 프로세스는 주기적으로 `wait()`를 호출해서 자신이 입양한 자식들을 거두어주므로, 고아 프로세스가 좀비가 되는 일은 없다.

### ps 명령어로 상태 확인하기

터미널에서 `ps aux` 명령을 실행하면 현재 실행중인 프로세스 목록을 볼 수 있다. `STAT` 열이 프로세스의 현재 상태를 나타낸다.

---

### 이후 학습

프로세스의 상태가 바뀌는 중요한 계기 중 하나는 바로 외부(다른 프로세서나 사용자 등)로부터의 '신호'다. 사용자가 `Ctrl+C`를 누르거나, 시스템이 비정상적인 상황을 알릴 때, 프로세스들은 'Signal'이라는 메커니즘을 통해 소통한다. 다음번에는 이 시그널이 뭔지, 어떻게 프로세스의 실행 흐름에 영향을 미치는지 알아봐야겠다.
