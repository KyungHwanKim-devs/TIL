### 스케줄러란?

스케줄러는 실제로 공부해보니 분량이 너무 많고 이해하기도 쉽지 않았다.....해서 내가 생각했을때 중요한 부분만 따로 정리했다.
한줄로 요약하자면 **어떤 프로세스에게 언제, 얼마나 CPU를 할당할지 결정하는 커널의 핵심 구성 요소**다.

Ready 상태의 프로세스들이 CPU를 기다리고 있을 때, 스케줄러는 그중 누구에게 CPU를 줄지 결정한다. 스케줄러는 한 프로세스에게 영원히 CPU를 주는게 아니라 아주 짧은 시간(보통 수 ms)만 주고 다시 뺏어서 다음 프로세스에게 준다.

### 왜 스케줄러가 필요한걸까?

옛날 초기 컴퓨터는 한 번에 하나의 프로그램만 실행할 수 있었다. 한 프로그램이 끝나야 다음 프로그램을 실행할 수 있었다. 이걸 **Single Programming**이라고 한다. 근데 이 방식은 매우 비효율적이었다.

그래서 나온 개념이 **Multitasking**이다. 한 프로세스가 I/O를 기다리는 동안, CPU를 다른 프로세스에게 주는 거다. 이렇게 하면 CPU를 최대한 효율적으로 만들 수 있다. 이 멀티태스킹을 실현하는게 바로 스케줄러의 역할이다.

- **CPU 사용률 극대화**: CPU가 놀지 않고 계속 일하게 만든다.
- **처리량 극대화**: 단위 시간당 완료되는 프로세스의 수를 늘린다.
- **응답 시간 최소화**: 사용자가 뭔가 입력했을 때 빠르게 반응한다.
- **공정성 보장**: 모든 프로세스가 CPU를 받을 수 있게 한다.

### 스케줄링 알고리즘들

스케줄러가 어떤 프로세스를 선택할지 결정하는 방식, 즉 스케줄링 알고리즘은 여러 가지가 있다.

#### 1. FCFS (First Come First Served)

가장 단순한 방식이다. 먼저 온 프로세스부터 순서대로 처리한다.

장점은 구현이 간단하고 공정하다는 거다. 단점은 앞에 긴 작업이 있으면 뒤의 짧은 작업들이 스위칭을 계속 기다려야 한다. 예를 들어 처리 시간이 1시간 걸리는 프로세스 A가 먼저 왔고, 그 뒤에 1초만 걸리는 프로세스 B, C, D가 기다리고 있다면, B, C, D는 1시간을 기다려야 한다. 이런 현상을 **Convoy Effect**라고 한다.

#### 2. SJF (Shortest Job First)

실행 시간이 가장 짧은 프로세스부터 처리한다. 평균 대기 시간을 최소화할 수 있다는 장점이 있다.

실무에서는 쓰기 어렵다. 해당 프로세스가 얼마나 실행될지 미리 알 수 없기 때문이다. 그리고 긴 작업은 계속 뒤로 밀려서 영원히 실행되지 못하는 **Starvation** 문제가 생길 수 있다.

#### 3. Round Robin

현대적인 시분할 시스템의 기초가 되는 알고리즘이다. 각 프로세스에게 동일한 시간(Time Quantum 또는 Time Slice, 보통 10~100ms)을 할당하고, 그 시간이 끝나면 다음 프로세스로 넘어간다.

예를 들어 Time Quantum이 10ms고 프로세스 A, B, C가 있다면:

1. A에게 10ms 할당 → A 실행
2. B에게 10ms 할당 → B 실행
3. C에게 10ms 할당 → C 실행
4. 다시 A에게 10ms 할당 → A 실행
5. 이런 식으로 계속 반복

장점은 모든 프로세스가 공정하게 CPU를 받는다. 단점은 Time Quantum을 어떻게 설정하느냐에 따라 성능이 크게 달라진다. 너무 짧으면 컨텍스트 스위칭이 너무 자주 일어나서 오버헤드가 크고, 너무 길면 FCFS와 비슷해진다.

#### 4. Priority Scheduling (우선순위 스케줄링)

각 프로세스에 우선순위를 부여하고, 우선순위가 높은 프로세스부터 실행한다. 시스템 프로세스는 높은 우선순위를, 일반 사용자 프로세스는 낮은 우선순위를 준다.

문제는 낮은 우선순위 프로세스가 영원히 실행되지 못하는 기아 현상이다. 이를 해결하기 위해 **Aging** 기법을 쓴다.

#### 5. CFS (Completely Fair Scheduler) - 리눅스의 현대 스케줄러

리눅스 커널 2.6부터 도입된 스케줄러다. 이름 그대로 "완전히 공정한" 스케줄링을 목표로 한다.

CFS의 핵심 아이디어는 **가상 실행 시간, Virtual Runtime, vruntime**이다. 각 프로세스가 실제로 CPU를 얼마나 사용했는지 추적하고, vruntime이 가장 작은(즉, CPU를 적게 사용한) 프로세스를 선택한다. 이렇게 하면 모든 프로세스가 "공정하게" CPU를 나눠 가질 수 있다.

CFS는 **Red-Black Tree** 자료구조를 사용해서 vruntime이 가장 작은 프로세스를 빠르게(O(log N)) 찾는다. 효율적이면서도 공정한 설계다.

### Context Switching

스케줄러가 실행중인 프로세스를 바꿀 때, 즉 CPU를 프로세스 A에서 B로 넘길 때 일어나는 일이 바로 **컨텍스트 스위칭**이다.

이 과정은 대략

1. 현재 실행중인 프로세스 A의 상태(레지스터 값, 프로그램 카운터 등)를 PCB에 저장한다.
2. 다음에 실행할 프로세스 B의 PCB에서 이전에 저장해둔 상태를 꺼내온다.
3. CPU의 레지스터들을 B의 상태로 복원한다.
4. B가 실행된다.

이 과정 자체는 실제 작업을 하는게 아니라 순수한 오버헤드다. 그래서 컨텍스트 스위칭이 너무 자주 일어나면 성능이 떨어진다. 스케줄러 설계의 핵심 중 하나가 이 오버헤드를 최소화하면서도 공정성과 응답성을 보장하는 거다.

스케줄러는 커널의 가장 핵심적인 부분 중 하나다. 단일 CPU 코어로도 수십 개의 프로세스가 '동시에' 실행되는 것처럼 느낄 수 있는건 순전히 스케줄러의 빠르고 공정한 작업 때문이다.

### 이후 학습 방향

프로세스가 실행되려면 CPU만 있으면 되는게 아니다. 코드와 데이터를 저장할 **메모리**가 필요하다. 내일은 메모리 관리에 대해 깊이 파봐야겠다.
