# 메모리

프로세스가 실행되려면 CPU만 있으면 되는게 아니다. 코드와 데이터를 어딘가에 저장해야 하는데, 그게 바로 **Memory**다. 메모리도 CPU처럼 한정된 자원이다. 8GB, 16GB 메모리에서 수백 개의 프로세스가 동시에 실행된다. 커널이 생각보다 작은 용량의 메모리를 어떻게 관리하는건지 아라보자

### 메모리 관리가 왜 필요한가

초기 컴퓨터는 한 프로그램만 실행했기 때문에 메모리 관리가 단순했다. 프로그램이 메모리 전체를 다 쓰면 끗. 근데 여러 프로그램을 동시에 실행하려면 문제가 생긴다.

- **메모리 부족**: 프로세스들이 요구하는 메모리를 다 합치면 물리 메모리보다 클 수 있다.
- **메모리 보호**: 한 프로세스가 다른 프로세스의 메모리 영역을 건드리면 안 된다. 만약 브라우저 프로세스가 텍스트 에디터 프로세스의 메모리를 덮어쓴다면?
- **메모리 단편화**: 프로세스가 생성되고 종료되기를 반복하면서 메모리에 빈 공간이 여기저기 흩어진다.

커널의 메모리 관리 시스템은 이런 문제를 여러 방법을 통해 해결 한다.

### 물리 메모리 vs 가상 메모리

**물리 메모리(Physical Memory)**는 실제로 컴퓨터에 꽂혀있는 RAM이다. ex 8GB 메모리

**가상 메모리(Virtual Memory)**는 각 프로세스가 보는 메모리 공간이다. **각 프로세스로 하여금 자기가 메모리 전체를 독점하고 있다고 착각하게 한다.** 32비트 시스템에서는 각 프로세스가 4GB의 메모리 공간을 가진다고 생각한다. 64비트 시스템에서는 훨씬 더 크다.

**가상 주소와 물리 주소**를 이용해서 이 개념을 구현할 수 있다.

프로세스가 메모리 주소 `A`에 접근한다고 해보자. 이건 가상 주소다. 커널(정확히는 MMU, Memory Management Unit이라는 하드웨어)은 이 가상 주소를 실제 물리 주소로 변환한다. 프로세스 A의 `A`은 물리 주소 `a`으로, 프로세스 B의 `A`은 물리 주소 `b`으로 매핑될 수 있다. 각 프로세스는 같은 주소를 쓴다고 생각하지만, 실제로는 다른 물리 메모리를 쓰는 거다. 이를 통해

- **메모리 보호**: 프로세스 A는 자기에게 할당된 가상 주소만 쓸 수 있다. 다른 프로세스의 메모리에는 접근할 수 없다.
- **메모리 확장**: 물리 메모리보다 큰 가상 메모리를 쓸 수 있다(스왑??을 이용해서).
- **메모리 공유**: 여러 프로세스가 같은 물리 메모리를 공유할 수 있다(예: 공유 라이브러리).

### 페이징 (Paging)

가상 주소를 물리 주소로 변환하는 가장 일반적인 방법이 **페이징**이다.

메모리를 **Page**라는 고정 크기의 블록으로 나눈다. 보통 4KB다. 가상 메모리도 페이지로, 물리 메모리도 페이지로 나눈다(물리 메모리의 페이지는 **Frame**이라고 부르기도 한다).

커널은 각 프로세스마다 **Page Table**이라는 자료구조를 유지한다. 이게 가상 페이지 번호를 물리 프레임 번호로 변환하는 Dictionary 역할을 한다.

예를 들면

- 프로세스가 가상 주소 `D`(가상 페이지 2)에 접근
- 페이지 테이블 확인: "가상 페이지 2는 물리 프레임 10에 매핑되어 있네"
- 물리 주소 `f`(물리 프레임 10)에서 데이터를 읽음

### Swap

물리 메모리가 부족할 때 어떻게 할까? 가장 오랫동안 사용되지 않은 페이지를 디스크로 일단 옮겨둔다. 이게 **Swap**이다.

실제 상황

1. 메모리가 부족하다
2. 커널이 가장 덜 사용된 페이지를 선택한다(LRU, Least Recently Used 알고리즘 등)
3. 그 페이지를 디스크의 스왑 영역에 저장한다
4. 해당 페이지의 페이지 테이블 엔트리를 업데이트한다(Present bit = 0)
5. 물리 메모리의 그 프레임을 다른 프로세스에게 준다

나중에 프로세스가 스왑된 페이지에 접근하려고 하면? **Page Fault**를 발생시킨다. 커널은 이 페이지 폴트가 발생하면

1. 디스크에서 페이지를 다시 읽어온다
2. 물리 메모리의 빈 프레임에 로드한다
3. 페이지 테이블을 업데이트한다
4. 프로세스가 계속 실행된다

해당 프로세스는 이 모든 과정을 전혀 모른다.

스왑이 너무 자주 일어나면 시스템 성능이 급격히 떨어진다. 디스크와 메모리 사이를 왔다갔다하느라 CPU가 실제 작업을 거의 못 한다. 이런 상태를 **Thrashing**이라고 한다.

#### free 명령어

가장 기본적인 메모리 확인 명령어다.

```bash
free -h
```

```text
               total        used        free      shared  buff/cache   available
Mem:            15Gi       4.2Gi       8.1Gi       256Mi       3.2Gi        10Gi
Swap:          2.0Gi          0B       2.0Gi
```

- **total**: 전체 물리 메모리
- **used**: 사용중인 메모리
- **free**: 완전히 비어있는 메모리
- **buff/cache**: 커널이 캐시로 쓰는 메모리(필요하면 바로 회수 가능)
- **available**: 실제로 사용 가능한 메모리
- **Swap**: 스왑 영역 사용량

중요한 건 `available` 값이다. `free`가 작아도 `available`이 크면 괜찮다. 커널이 필요하다면 buff/cache에 할당된 메모리를 해제하고 사용할 수 있다.

메모리 관리는 운영체제의 가장 복잡한 부분 중 하나다. 가상 메모리라는 개념 덕분에 우리는 물리 메모리보다 큰 프로그램을 실행할 수 있고, 여러 프로세스가 안전하게 메모리를 나눠 쓸 수 있다. 페이징과 스왑 같은 메커니즘이 없었다면 현대적인 멀티태스킹 시스템은 불가능했을것 같다.

### 이후 학습 방향

메모리 관리의 기본은 알았는데, 사실 가상 메모리를 깊이 파보면 더 복잡한 이야기들이 많다. 페이지 테이블이 어떻게 구성되어 있는지, TLB(Translation Lookaside Buffer)는 뭔지, 멀티레벨 페이징은 왜 필요한지 등 아직 모르는게 많다. 다음번부터는 가상 메모리의 내부 구조를 더 깊이 파봐야겠다.
