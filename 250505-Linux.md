# 리눅스란 무엇인가?

한동안 회사일이 바빠서 학습이 뜸했다. 이제 바쁜게 끝나서 다시 기본기를 위해 공룡책을 포함해 OS, CS, Network에 관련된 서적도 준비해서 읽기 시작했다.
먼저 저번에 libuv를 공부하면서 쓰레드, I/O, 시스템 콜 등 리눅스에 관련된 용어들이 많이 나와서 깊이있는 이해가 조금 힘들었다.
그래서 개발자라면 당연히 알아야할 주제중 하나인 리눅스를 공부하기로 결정했다.
지금까지는 당장 실무에서 사용해야할 리액트, 넥스트, 네스트 같은 프레임워크나 해당 프레임워크에서 사용할 라이브러리를 사용하는데 급급했었고,
리눅스는 막연히 언젠간 공부 해야겠다고 생각만 했었는데, 이번 기회를 통해 좀 더 깊이있는 학습을 하기 위해 리눅스와 컴퓨터 사이언스, 네트워크에 대해서 기본기부터 단단하게 다져봐야겠다.
한번에 리눅스의 모든것을 공부하긴 어려우니 전반적으로 리눅스의 구성요소들을 한번 훑어본 다음에 각 요소를 파봐야 겠다.

---

### 간단한 리눅스의 역사

먼저 리눅스가 개발되기 전에, 미국의 벨 연구소에서 만든 유닉스가 현대 운영체제의 기반을 만들었고, 곧 유닉스는 상업용 독점 소프트웨어가 되었다.
이후 80년대 초, 리처드 스톨만은 소프트웨어의 상업화에 반대하며 GNU 프로젝트를 통해 무료로 배포할 수 있는 운영체제를 만들고자 했고, 이를 통해 GCC 컴파일러, 쉘 등의
핵심 유틸리티는 만들었지만 운영체제의 핵심이라 할 수 있는 `커널` 개발은 완성하지 못하고 있었다.
90년대 초에 와서 리누스 토르발스라는 대학생이 취미삼아 커널을 개발해서 오픈소스로 공개해서 전세계 개발자들의 개발 참여를 유도했고,
결국 GNU 프로젝트와 리눅스 커널의 결합을 통해 완전한 자유-오픈 소스의 운영체제가 탄생하게 되었다.

### 리눅스의 설계 철학

리눅스는 그 뿌리가 되는 유닉스의 강력하고 간결한 설계 철학을 그대로 계승했다. 이 철학을 이해하면 리눅스의 수많은 명령어와 도구들이 왜 그렇게 만들어졌는지 알 수 있다.

- **모든 것은 파일이다 (Everything is a file)**: 리눅스의 가장 핵심적인 철학이다. 일반적인 텍스트 파일이나 디렉터리뿐만 아니라, 키보드, 마우스 같은 하드웨어 장치(`dev/`), 실행 중인 프로세스에 대한 정보(`proc/`) 등 시스템의 거의 모든 것이 파일로 추상화되어 있다. 이 덕분에 개발자는 `open()`, `read()`, `write()` 같은 표준 파일 처리 함수만으로 시스템의 다양한 자원을 일관된 방식으로 다룰 수 있다.

- **작고, 뾰족한 도구들을 조합하라 (Small, sharp tools)**: 하나의 프로그램은 하나의 작업만 잘하도록 만들고, 이런 작은 프로그램들을 '파이프(`|`)'로 연결하여 더 복잡한 작업을 수행하라는 철학이다. 예를 들어, `cat`으로 파일 내용을 출력하고, `grep`으로 특정 문자열을 찾고, `sort`로 정렬하는 식이다. 이 방식은 거대한 단일 프로그램을 만드는 것보다 훨씬 유연하고 강력하다.

- **가능한 빨리 프로토타입을 만들고, 쉘 스크립트를 활용하라**: 쉘은 단순히 명령어를 입력하는 공간을 넘어, 그 자체로 강력한 프로그래밍 환경이다. 복잡한 로직도 쉘 스크립트로 빠르게 자동화하여 테스트하고 개선할 수 있다.

---

### 리눅스의 핵심 구성요소

사실 좁은 의미의 '리눅스' 라고 하면 커널(Kernel) 그 자체라고 한다. 커널이 무엇인지는 바로 다음에 자세히 공부해보기로 하고,
일상적으로 사용하는 넓은 의미의 '리눅스'라고 하면 커널(Kernel)과 함께 GNU 프로젝트의 라이브러리(glibc), 셸(bash, zsh),
시스템 도구(coreutils, systemd), 패키지 관리자(apt, yum, pacman) 등이 같이 포장된 '리눅스 배포판'을 지칭한다.

### 주요 리눅스 배포판

앞서 언급했듯, 우리는 순수한 커널이 아닌 '리눅스 배포판'을 설치해서 사용한다. 배포판은 리눅스 커널에 GNU 소프트웨어와 각종 유틸리티, 데스크톱 환경(GNOME, KDE 등) 등을 조합하여 일반 사용자가 쉽게 설치하고 사용할 수 있도록 만든 패키지다. 수많은 배포판이 존재하는 이유는 저마다 다른 철학과 목적을 가지고 있기 때문이다.

- **데비안 계열 (Debian-based)**: 안정성을 최우선으로 생각하는 배포판의 대부. `.deb` 패키지 형식과 `apt`라는 강력한 패키지 관리 도구를 사용한다.
  - **우분투 (Ubuntu)**: 데비안을 기반으로 '사용자 친화성'을 극대화하여 데스크톱 시장에서 가장 큰 성공을 거둔 배포판.
- **레드햇 계열 (Red Hat-based)**: 주로 기업 및 서버 환경에서 강력한 지지를 받는 계열. `.rpm` 패키지 형식과 `yum`, `dnf` 패키지 관리자를 사용한다.
  - **페도라 (Fedora)**: 레드햇이 후원하는 커뮤니티 기반 배포판으로, 최신 기술을 빠르게 도입하고 테스트하는 역할을 한다.
  - **CentOS / Rocky Linux**: 레드햇 엔터프라이즈 리눅스(RHEL)와 거의 완벽하게 호환되는 무료 버전으로, 안정적인 서버를 구축할 때 많이 사용된다.
- **아치 리눅스 (Arch Linux)**: '단순함'과 '사용자 중심'을 철학으로, 사용자가 시스템의 모든 것을 직접 설정하고 제어하기를 권장하는 배포판. 미니멀리즘과 커스터마이징을 선호하는 숙련된 사용자들에게 인기가 많다.

---

### 리눅스가 하는일?

그렇다면 커널을 중심으로 한 리눅스는 구체적으로 어떤 일을 할까? 이 모든 강력한 기능에 사용자는 어떻게 접근할 수 있을까? 바로 여기서 **셸(Shell)**이 등장한다. 셸은 사용자가 입력한 명령을 해석하여 커널에 전달해주는 '인터페이스'다. 우리가 터미널에서 `ls`, `cd` 같은 명령어를 치면, 셸이 그 명령을 받아 커널에게 "이런이런 작업을 처리해달라"고 요청하는 것이다.

결국 리눅스가 하는 일은 무엇이냐? 한줄로 요약하자면 '커널과 각종 라이브러리, 시스템 도구들을 통해 하드웨어 자원을 효율적이고 안전하게 관리하고,
사용자 프로그램이 일관성 있게 실행될 수 있도록 기반을 제공하는 것'이라고 할 수 있다.
리눅스의 본질은 커널 그 자체 이며, 그 역할은 하드웨어와 사용자가 만든 소프트웨어 사이에서 중간자 역할을 하는 것이다.
사용자가 실행하는 응용 프로그램은 직접 하드웨어(`CPU`, `메모리`, `저장장치`, `네트워크 통신` 등)에 접근할 수 없기 때문에(해서도 안되고....),
리눅스 커널이 그 사이에서 `시스템 콜`등의 요청을 통해 적절히 처리한다.

예를 들어, 어떤 프로그램이 파일을 열고 싶다고 요청하면 커널은 `파일 시스템`을 통해 저장장치의 `드라이브`에 접근하고, 결과를 프로그램에 돌려준다.
또 여러 프로그램이 동시에 CPU를 사용하려 할 때, 커널은 `스케줄링`을 통해 어떤 `프로세스`가 언제, 얼마나 CPU를 사용할지 결정한다. 메모리가 부족하면 커널은 불필요한 페이지를 디스크로 `메모리스왑`하거나,
`캐시`를 정리해 메모리를 확보한다. 또한 `네트워크 패킷`이 도착하면 `인터럽트`를 일으켜 `네트워크 스택`을 통해 데이터를 처리하고, 이를 해당 프로세스에 전달한다.

즉, 리눅스는 `프로세스 관리`, `메모리 관리`(`가상 메모리`, `페이지`), `파일 시스템 관리`(디스크 I/O), `장치` 관리(`드라이버`를 통한 하드웨어 제어), 네트워킹(`소켓`, 패킷 처리) 등
운영체제에 필요한 거의 모든 핵심 기능을 맡는다. 이 모든 과정은 사용자가 직접 느끼지 못하는 깊은 곳에서 동작하며, 안정적이고 보안성 있는 환경 위에서 응용 프로그램이 실행될 수 있도록 보장하는 역할을 하는것이다.

특히 현대에 와서 `클라우드`의 폭발적인 성장은 리눅스의 중요성을 더욱 부각시켰다.
클라우드 환경에서는 수백, 수천 대의 `가상 머신(VM)`과 `컨테이너`가 빠르게 생성되고 사라진다. 이런 대규모 환경을 안정적으로 운영할 수 있는 운영체제가 바로 리눅스다.
AWS 같은 클라우드 서비스들이 운영하는 수많은 서버는 대부분 리눅스를 기반으로 돌아간다.

또한 컨테이너는 리눅스 커널의 핵심 기능인 네임스페이스(namespaces) 와 cgroups(control groups) 에 의존하여,
격리된 환경에서 애플리케이션을 효율적으로 실행할 수 있게 한다(사실 이 부분은 개인적으로는 좀 충격적이었다. 개발 경력 시작 자체를 클라우드와 컨테이너가 대중화 된 이후에 시작해서
너무 자연스럽게 ec2 등의 서비스와 도커 같은 컨테이너 런타임을 사용하고 있었고, 그 기반이 VM이나 리눅스의 기능들의 조합인줄은 생각도 못하고 있었다).
따라서 현대 개발자에게 리눅스는 단순히 서버 관리 도구를 넘어, 클라우드 네이티브 애플리케이션 개발과 운영의 기본 역량이 되었다.

### 이후 학습 방향

리눅스의 전반에 대해서 한번 조망해봤다. 이제 바로 위의 단락에서 리눅스가 하는일을 요약하면서 나온 키워드들을 기반으로 공부를 시작해봐야 겠다.
다음엔 커널과 그와 관련된 개념, 기능들을 깊이있게 공부해 봐야겠다.
