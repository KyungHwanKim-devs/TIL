# NodeJs란 무엇인가?

오늘은 내가 현재 일하고 있는 포지션인 Javascript 백엔드 개발에서 사용하는 런터임인 NodeJs에 대해 깊이 대해서 전반적으로 알아보려 한다.
이번 글 에서는 깊이 보단 중요한 요소들을 추려보고 하나씩 깊이있게 공부해 볼 예정이다.

일단 NodeJs를 검색하면 가장 많이 나오는 'Chrome V8 JavaScript 엔진으로 빌드된 JavaScript 런타임', '싱글 스레드의 이벤트 기반 논 블럭킹I/O' 모델이라고 소개된다. 이 단어들 먼저 해석해보자

먼저 '런타임'이란 말은 '특정 언어로 만든 프로그램들을 실행할 수 있는 환경'정도로 요약 할 수 있다. 자바스크립트는 최초에는 브라우저에서 유저 인터렉션을 위한 언어로 탄생했고 그래서 유일한 런타임은 웹 브라우저였지만, NodeJs 이라는 런타임이 생긴 덕분에 자바스크립트는 이제 서버처럼 브라우저가 없는 환경에서도 실행될 수 있는 언어가 되었다.

NodeJs가 브라우저 밖에서도 강력한 성능을 발휘할 수 있는 이유는 바로 V8 엔진 때문이다. V8 엔진은 구글이 개발한 고성능 자바스크립트 엔진으로, 코드를 실행하는 시점에 바로 빠른 속도의 기계어로 컴파일(JIT, Just-In-Time Compilation)한다.
결국 NodeJs에는 이 V8 엔진 덕분에, 느리다는 편견이 있던 자바스크립트만으로도 고성능 서버를 만들 수 있는 강력한 런타임이 되었다.

그렇다면 **싱글 스레드와 이벤트 기반 논블로킹 I/O** 는 또 무엇이냐. 이 부분이 NodeJs에의 아키텍처를 이해하는 가장 중요한 핵심이다. 하나하나 뜯어보자.

- **싱글 스레드 (Single Thread)**
  NodeJs에는 자바스크립트 코드를 실행하는 메인 스레드가 **하나**다. 전통적인 멀티 스레드 서버가 사용자 요청마다 새로운 스레드를 만드는것과 대조된다(물론 최근에는 자바에서도 WebFlux나 가상스레드 등으로 논블로킹을 구현 한다). 그럼 도대체 "스레드가 하나인데 어떻게 많은 요청을 **동시에** 처리하지?" 라고 생각할 수 있다. 이 질문에 대한 답이 바로 '논블로킹 I/O와 이벤트 루프'다.

- **논블로킹 I/O (Non-blocking I/O)**
  시간이 오래 걸리는 I/O 작업(파일 읽기, DB 조회, API 요청 등)을 처리하는 방식이다.

  - **블로킹 방식**: 작업이 끝날 때까지 스레드가 멈춰서 기다린다. 이 시간 동안 스레드는 아무 일도 못 한다.
  - **논블로킹 방식**: 작업을 요청한 직후, 완료될 때까지 기다리지 않고 **즉시 다음 코드를 실행**한다. 작업이 끝나면 나중에 알림을 받아 처리한다.

- **이벤트 기반 (Event-driven)**
  NodeJs에는 논블로킹 I/O로 시작된 작업들의 완료 이벤트를 처리하기 위해 **이벤트 루프** 라는 걸 사용한다. 이벤트 루프는 계속 루프를 돌면서 실행할 작업이 있는지 확인하고, 있다면 순서대로 처리한다.

이 세 가지 요소가 작동하는 방식을 간단히 정리하면 다음과 같다.

1.  사용자 요청이 들어오면 NodeJs에의 싱글 스레드가 작업을 가져간다.
2.  만약 작업이 시간이 오래 걸리는 I/O라면, NodeJs에는 해당 작업을 libuv에 떠넘기고 **콜백 함수를 등록**한 뒤, 곧바로 다음 요청을 처리하러 간다. 이게 **논블로킹**
3.  떠넘겼던 I/O 작업이 완료되면, 등록해 둔 콜백 함수가 **태스크 큐(Task Queue)**에 들어간다.
4.  **이벤트 루프**는 콜 스택이 비어있을 때 태스크 큐를 확인하고, 대기 중인 콜백 함수를 가져와 싱글 스레드에서 실행시킨다. 이게 **이벤트 기반**

#### 코드 비교

파일을 읽는 간단한 코드로 두 방식의 차이를 보자.

**블로킹 (동기) 방식**

```javascript
const fs = require("fs");

console.log("시작");

const data = fs.readFileSync("./readme.md"); // 파일 읽기가 끝날 때까지 이 시점에서 대기
console.log("1번", data.toString());

console.log("끝");

// 출력 결과:
// 시작
// 1번 ... (파일 내용)
// 끝
```

**논블로킹 (비동기) 방식**

```javascript
const fs = require("fs");

console.log("시작");

fs.readFile("./readme.md", (err, data) => {
  // 파일 읽기가 끝나면 이 콜백 함수가 태스크 큐에 들어감
  console.log("1번", data.toString());
});

console.log("끝");

// 출력 결과:
// 시작
// 끝
// 1번 ... (파일 내용)
```

논블로킹 방식에서는 `readFile`을 실행시킨 후 기다리지 않고 바로 `console.log("끝")`으로 넘어간다. 그동안 백그라운드에서 파일 읽기가 끝나면, 등록해둔 콜백 함수가 실행되어 파일 내용이 출력된다. 이것이 NodeJs에가 단일 스레드로도 높은 처리량을 가질 수 있는 비결이다. 다만 보통은 I/O 작업만 떠넘기기 때문에
I/O 작업이 아닌 경우는 스레드가 직접 CPU 사용을 통해 작업을 경우 이 동안은 블럭킹 된다. 물론 이런경우 libuv를 통해 별도의 스레드를 통해 작업을 하도록(Worker Threads)만들어서 처리 할 수도 있다. 심지어 child_process를 통해서 멀티 프로세싱을 통해 자식 프로세스에게 떠넘길수도 있다. 개인적으로는 이런 부분들 때문에 처음에 '싱글 스레드의 논 블럭킹I/O'라는 키워들이 굉장히 이해하기 어려웠다.
물론 실제로는 이보다 훨씬 복잡한, 콜스택, 마이크로큐, 매크로큐 등으로 구성된 이벤트루프 등 구조와 과정들이 숨어있다. 오늘은 전체적인 그림을 그려봤으니, 다음번에는 NodeJs에의 심장이라고 할 수 있는 **libuv**에 대해 더 깊이 파고들어 봐야겠다.
