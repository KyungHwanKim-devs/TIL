# 프로세스란 무엇인가?

어제 시스템 콜을 공부하면서 `fork()`, `execve()`, `getpid()` 등 `프로세스`라는 단어가 계속 나왔다. 커널이 관리하는 가장 중요한 대상이 프로세스라고 했는데, 정작 프로세스가 정확히 뭔지는 막연하게만 알고 있었던 것 같다. "실행 중인 프로그램" 정도로만 알고 있었는데, 이번 기회에 운영체제가 프로세스를 어떻게 바라보고 관리하는지 제대로 파봐야겠다.

### 프로그램 vs 프로세스

그럼 프로그램과 프로세스는 뭐가 다른걸까?

- **프로그램 (Program)**: 디스크에 저장된, 아직 실행 안된 코드 덩어리다. 예를 들어 `/bin/ls` 같은 바이너리 혹은 실행 파일 그 자체를 의미한다.
- **프로세스 (Process)**: 프로그램이 실제로 메모리에 올라와서 CPU에 의해 실행되고 있는 상태를 말한다.

객체지향처럼 프로그램은 '객체'고, 프로세스는 '인스턴스' 라고 할 수 있다.

### 프로세스의 메모리 구조

프로세스는 실행되기 위해 커널로부터 독립적인 메모리 공간을 할당받는다.

- **Text (Code) 영역**: 실행할 프로그램 코드가 저장되는 영역이다. 읽기 전용(Read-Only)으로, 프로그램 실행되는 동안 코드가 변경되지 않도록 보호된다.
- **Data 영역**: 전역 변수나 정적(static) 변수처럼, 프로그램이 시작할 때 이미 값이 정해져 있고 끝날 때까지 유지되는 데이터가 저장된다.
- **BSS (Block Started by Symbol) 영역**: 초기화되지 않은 전역 변수나 정적 변수가 저장된다.
- **Heap 영역**: 개발자가 직접 관리하는 메모리 영역이다. `malloc()`이나 `new` 로 동적으로 메모리를 할당하고 `free()`나 `delete`로 해제한다. 힙은 낮은 주소에서 높은 주소 방향으로 자란다.
- **Stack 영역**: 함수 호출 시 사용되는 지역 변수, 매개변수, 리턴 주소 등이 저장된다. 함수 호출이 시작되면 스택 프레임(Stack Frame)이 쌓이고, 함수가 끝나면 사라진다. 스택은 높은 주소에서 낮은 주소 방향으로 자란다.

### 프로세스 제어 블록 (PCB, Process Control Block)

커널은 수많은 프로세스를 관리하기 위해 각 프로세스에 대한 모든 핵심 정보를 담고 있는 특별한 자료구조를 유지하는데, 이게 바로 **PCB**다. 커널은 프로세스 그 자체를 직접 다루는게 아니라, 이 PCB의 목록을 보고 시스템 전체를 관리한다. PCB에는 다음과 같은 정보들이 담겨있다.

- **프로세스 ID (PID)**: 각 프로세스를 식별하는 고유한 번호.
- **프로세스 상태**: 현재 프로세스가 Ready, Running, Waiting 중 어떤 상태인지 나타낸다.
- **프로그램 카운터**: 이 프로세스가 다음에 실행할 명령어의 주소를 가리킨다.
- **CPU 레지스터**: 이 프로세스가 이전에 실행될 때 사용했던 레지스터 값들의 스냅샷.
- **메모리 관리 정보**: 이 프로세스가 할당받은 메모리 공간(Text, Data, Stack 등)에 대한 정보.
- **입출력 상태 정보**: 프로세스에 할당된 파일이나 입출력 장치 목록.

컨텍스트 스위칭이 일어날 때, 커널은 현재 실행중인 프로세스의 모든 상태를 PCB에 저장하고, 새로 실행할 프로세스의 정보를 PCB에서 꺼내와 CPU에 로드한다.

### 프로세스 관계: 포어그라운드, 백그라운드, 데몬

#### 포어그라운드와 백그라운드 프로세스

- **포어그라운드 프로세스 (Foreground Process)**: 쉘에서 명령어를 입력하면, 기본적으로 그 프로세스는 포어그라운드로 실행된다. 이 프로세스는 터미널을 차지하고 사용자로부터 직접 입력을 받을 수 있으며, 실행이 끝날 때까지 쉘은 다른 명령을 받지 못하고 기다린다. `Ctrl+C` 같은 인터럽트 시그널(???)은 포어그라운드 프로세스에 전달된다.

- **백그라운드 프로세스 (Background Process)**: 명령어 뒤에 `&`를 붙이면 프로세스는 백그라운드로 실행된다. 이 프로세스는 터미널에 묶이지 않고 뒤에서 조용히 실행되며, 쉘은 즉시 다음 명령을 받을 수 있는 상태가 된다. `jobs` 명령으로 백그라운드 작업을 확인하고, `fg`나 `bg` 명령으로 포어그라운드/백그라운드 상태를 전환할 수 있다.

#### 프로세스 그룹 (Process Group)

**프로세스 그룹**은 하나 이상의 관련된 프로세스들의 모음이며, 각 그룹은 고유한 ID(PGID)를 가진다.

이게 필요한 가장 대표적인 예는 파이프라인(`|`)이다. `ps -ef | grep nginx` 라는 명령을 실행하면, `ps`와 `grep` 두 개의 프로세스가 생성되는데, 이 둘은 같은 프로세스 그룹에 속하게 된다. 이렇게 그룹으로 묶어두면 커널은 그룹 전체에 한 번에 시그널을 보낼 수 있다. 예를 들어 `Ctrl+C`를 누르면 쉘은 파이프라인으로 연결된 모든 프로세스가 속한 그룹 전체에 `SIGINT` 시그널(.....)을 보내 작업을 한꺼번에 중단시킨다.

#### 세션 (Session)

프로세스 그룹보다 한 단계 더 큰 개념이 **세션**이다. 세션은 사용자가 시스템에 로그인해서 로그아웃할 때까지의 작업 단위를 의미하며, 하나 이상의 프로세스 그룹으로 구성된다.

사용자가 터미널을 열고 로그인하면 새로운 세션이 시작되고 로그인 쉘이 '세션 리더'가 된다. 이 쉘에서 실행되는 모든 명령어(프로세스 그룹들)는 해당 세션에 속하게 된다. 만약 사용자가 터미널을 닫거나 로그아웃하면, 커널은 세션 전체에 `SIGHUP`(Hang Up?...) 시그널을 보내 세션에 속한 모든 프로세스를 종료시킨다.

#### 데몬 (Daemon) 프로세스

데몬은 **세션에 종속되지 않고, 제어 터미널도 없이** 백그라운드에서 실행되는 특별한 프로세스다. 이들은 보통 시스템이 부팅될 때 `init` 프로세스(PID 1)에 의해 실행되어 시스템이 종료될 때까지 특정 서비스를 제공하는 역할을 한다. `nginx`, `mysqld` 등이 대표적인 데몬 프로세스다. 데몬은 터미널로부터 `SIGHUP` 시그널을 받을 일이 없으므로 사용자의 로그인 여부와 관계없이 계속 살아있을 수 있다.

'프로세스'는 독립적인 메모리 공간과 생명 주기를 가지며, 커널에 의해 PCB라는 '신분증'을 통해 체계적으로 관리되는 운영체제의 핵심 작업 단위였다. 컴퓨터에서 여러 프로그램을 동시에 실행할 수 있는 것도, 커널이 이 프로세스들을 빠르고 공정하게 번갈아 가며 실행(컨텍스트 스위칭?)시켜주기 때문이라는걸 확인했다.

### 이후 학습 방향

이제 프로세스가 뭔지, 그리고 커널이 어떻게 프로세스를 관리하는지에 대한 대략적인 그림이 그려졌다. 그럼 이 프로세스는 처음에 어떻게 만들어지는가? 시스템 콜 글에서 잠깐 나왔던 `fork()`와 `execve()`로 프로세스를 만든다고 했다. 내일은 이 두 시스템 콜이 어떻게 리눅스(유닉스?)의 모든 프로세스를 탄생시키는지 그 과정을 파헤쳐 봐야겠다.
