### TLB -Translation Lookaside Buffer

페이지 테이블을 메모리에 저장하면 문제가 생긴다. 메모리에 접근할 때마다 페이지 테이블도 읽어야 하니까 메모리 접근이 2배가 된다.

역시나 해결책은 **캐시**다. 최근에 변환한 주소를 빠른 하드웨어에 캐싱 해두는게 바로 **TLB(Translation Lookaside Buffer)**다.

TLB는 CPU 내부에 있는 아주 빠른 캐시다. 크기는 작지만(보통 수십~수백 개 엔트리) 속도가 엄청나게 빠르다.
프로세스가 메모리에 접근할 때:

1. 먼저 TLB를 확인한다. 가상 주소의 변환 정보가 있나?
2. **TLB Hit**: 있다! 바로 물리 주소로 변환하고 메모리에 접근 (매우 빠름)
3. **TLB Miss**: 없다! 페이지 테이블을 메모리에서 읽어야 한다. (느림)
   - 페이지 테이블에서 변환 정보를 찾는다
   - TLB에 이 정보를 저장한다 (다음번에는 빠르게 접근 가능)
   - 물리 주소로 접근한다

프로그램은 일반적으로 **공간 지역성(Spatial Locality)**과 **시간 지역성(Temporal Locality)**을 가진다. 최근에 접근한 메모리 `근처`를 다시 접근하고, 같은 메모리를 `반복`해서 접근한다는 뜻이다. 보통 TLB Hit Rate가 95% 이상으로 측정된다. 이 때문에 결국 작은 크기의 TLB만으로도 엄청난 성능 향상을 얻을 수 있다.

### 페이지 테이블 엔트리의 플래그

- **Present (P) bit**: 이 페이지가 현재 물리 메모리에 있는가? 0이면 스왑 아웃되었거나 아직 할당되지 않은 상태다. 접근하면 Page Fault가 발생
- **Read/Write (R/W) bit**: 쓰기 가능한가? 0이면 읽기 전용이다. Code 영역이 대표적이다. 쓰기를 시도하면 Segmentation Fault가 발생
- **User/Supervisor (U/S) bit**: 유저 모드에서 접근 가능한가? 0이면 커널 모드에서만 접근 가능하다. 유저 프로세스가 커널 메모리에 접근하려 하면 막힌다.
- **Accessed (A) bit**: 이 페이지에 접근한 적이 있는가? LRU 같은 페이지 교체 알고리즘에 사용된다.
- **Dirty (D) bit**: 이 페이지가 수정되었는가? 스왑 아웃할 때 디스크에 쓸 필요가 있는지 판단한다. 읽기만 했다면 굳이 쓸 필요가 없다.

리눅스에서 TLB 통계를 직접 보는건 안되지만, 간접적으로 페이지 폴트 발생 횟수를 확인할 수 있다고 한다.

```bash
# 현재 프로세스의 메모리 통계
cat /proc/self/status
```

```text
...
NSpgid:	257385
NSsid:	229671
VmPeak:	    5912 kB <-이게 페이비 폴트 통계??
VmSize:	    5912 kB
VmLck:	       0 kB
...
```

### 이후 학습 방향

메모리의 구조는 조금 알겟는데, 실제로 프로그램이 메모리를 어떻게 할당받는지는 아직 구체적으로는 모르겠다. C언어의 함수중에서 `malloc()`을 호출하면 개발자가 직접 메모리를 할당 한다고 하는데 내부적으로 무슨 일이 일어나는지는 아직 모르겠다. 힙은 어떻게 관리되고, 메모리 누수는 왜 생기는지 다음번에 파봐야겠다.
