# 시스템 콜

저번 커널 글 마지막에, NodeJs 공부할때부터 계속 나왔던 `시스템 콜`에 대해 알아보기로 했었다.
커널이 유저 공간의 애플리케이션과 하드웨어 사이에서 중재자 역할을 한다고 했는데, 시스템 콜이 바로 그 중재를 위한 공식적인 통로라고 할 수 있다. 이번에는 시스템 콜이 뭔지, 왜 필요한지, 어떻게 동작하는지에 대해 정리해보려 한다.

### 시스템 콜이란?

한줄로 요약하자면 **실행 중인 프로그램(프로세스)이 운영체제 커널의 기능을 쓰기 위해 호출하는 인터페이스**다.
저번에 공부했듯이, 유저 모드에서 실행되는 일반 프로그램들은 시스템 안정성과 보안을 위해 하드웨어나 커널 메모리에 직접 접근할 수 없다. 파일을 조작하거나, 네트워크로 데이터를 보내거나, 새로운 프로세스를 만드는 등 특권이 필요한 작업은 반드시 커널에게 요청해야 한다. 시스템 콜이 바로 이 '요청'을 전달하는 유일한 방법이다.

근데 사실 애플리케이션은 시스템 콜을 직접 부르는게 아니라 보통 `C 라이브러리(glibc)` 같은 고수준 API를 통해 간접적으로 호출한다. 예를 들어 C에서 `printf()` 함수를 쓰면, 내부적으로는 `write()`라는 시스템 콜을 호출해서 화면에 텍스트를 출력하는 식이다. C 라이브러리 함수들은 시스템 콜을 한 번 감싸는 '래퍼(Wrapper) 함수' 역할을 하는 거다. 이 래퍼 함수 덕분에 우리는 복잡한 레지스터 조작이나 인터럽트 같은 저수준 과정을 몰라도, 익숙한 함수 호출 방식으로 커널 기능을 편하게 쓸 수 있다.

### 시스템 콜의 동작 과정

그럼 프로그램이 시스템 콜을 호출하면 내부적으로는 어떤 일이 일어나는걸까? 전체적인 흐름을 정리해보자.

1.  **유저 프로세스가 라이브러리 함수를 호출한다.** (예: `open()`, `read()`)
2.  라이브러리 함수는 시스템 콜 번호를 특정 `레지스터`에 저장하고, 필요한 인자들도 다른 레지스터에 배치한다.
3.  라이브러리 함수가 `소프트웨어 인터럽트`(`syscall`)를 발생시킨다.
4.  CPU는 `인터럽트`를 감지하고, 현재 실행중인 프로세스의 상태를 저장한 뒤 `유저 모드`에서 `커널 모드`로 전환한다.
5.  커널은 `인터럽트 벡터 테이블`을 참조해서 해당 인터럽트를 처리할 `시스템 콜 핸들러`를 찾는다(이건 또 뭔지 모르겠네...).
6.  `시스템 콜 핸들러`는 레지스터에 저장된 시스템 콜 번호를 보고, `시스템 콜 테이블`에서 맞는 커널 함수를 찾아 실행한다.
7.  요청된 작업(예: 파일 읽기)이 끝나면, 결과는 다시 레지스터를 통해 반환되고, CPU는 `커널 모드`에서 `유저 모드`로 다시 전환한다.
8.  유저 프로세스는 멈췄던 지점부터 다시 실행되고, 라이브러리 함수는 커널로부터 받은 결과를 최종적으로 반환한다.

이 과정의 핵심은 **모드 전환(Mode Switch)**이다. 컨텍스트 스위칭까지는 아니더라도, 유저 모드와 커널 모드를 왔다갔다 하는 것 자체에도 어느 정도 오버헤드가 발생한다.

### 주요 시스템 콜의 종류

리눅스에는 수백 개의 시스템 콜이 있는데, 기능에 따라 크게 몇 가지로 분류할 수 있다.

- **프로세스 제어**: `fork()`(프로세스 생성), `execve()`(프로그램 실행), `exit()`(프로세스 종료), `wait()`(자식 프로세스 기다리기)
- **파일 관리**: `open()`(파일 열기), `read()`(파일 읽기), `write()`(파일 쓰기), `close()`(파일 닫기)
- **장치 관리**: `ioctl()`(장치 제어)
- **정보 유지**: `getpid()`(프로세스 ID 얻기), `gettimeofday()`(시간 정보 얻기)
- **통신**: `socket()`(소켓 생성), `connect()`, `accept()`, `send()`, `recv()`
- **메모리 관리**: `brk()`, `mmap()` (메모리 할당 및 매핑)

이 중에서 특히 유닉스 계열 시스템의 핵심이 되는 시스템 콜들이 있다. `fork()`와 `execve()`는 새로운 프로세스를 만들고 실행하는 근간이 되고, 쉘이 사용자 명령을 처리하는 방식의 기반이 된다. 또 프로세스가 실행 중에 동적으로 메모리를 할당받을 때 사용하는 `mmap()` 같은 메모리 관리 시스템 콜도 중요하다. C언어의 `malloc()` 같은 함수도 내부적으로는 이런 시스템 콜을 써서 커널에 메모리를 요청한다.

### strace로 시스템 콜 직접 확인해보기

이론은 이제 충분히 봤으니, 실제로 우리가 실행하는 프로그램이 어떤 시스템 콜을 호출하는지 직접 확인해보자. 리눅스에는 `strace`라는 강력한 도구가 있다. `strace`는 특정 프로그램이 실행될 때 발생하는 모든 시스템 콜과 인자, 반환값까지 추적해서 보여준다.

간단한 `ls` 명령어가 어떤 시스템 콜을 쓰는지 한번 추적해보자. 터미널에 다음과 같이 입력하면 된다.

```bash
strace ls
```

그러면 정말 엄청난 양의 텍스트가 쏟아지는데, 그중 몇 가지만 살펴보면 다음과 같다.

```text
execve("/bin/ls", ["ls"], 0x7ffc3b687b10 /* 55 vars */) = 0
brk(NULL)                               = 0x55c861a5e000
mmap(NULL, 8192, PROT_READ|PROT_WRITE, MAP_PRIVATE|MAP_ANONYMOUS, -1, 0) = 0x7f0b5f13b000
openat(AT_FDCWD, "/etc/ld.so.cache", O_RDONLY|O_CLOEXEC) = 3
...
fstat(1, {st_mode=S_IFCHR|0620, st_rdev=makedev(0x88, 0), ...}) = 0
write(1, "250325.md  250505-Linux.md ...\n", 42) = 42
...
close(1)                                = 0
exit_group(0)                           = ?
```

- `execve("/bin/ls", ...)`: 쉘이 `fork()`로 자식 프로세스를 만든 뒤, 그 자식이 `ls` 프로그램을 실행하기 위해 `execve` 시스템 콜을 호출하는걸 볼 수 있다.
- `brk()`, `mmap()`: `ls` 프로그램이 실행에 필요한 메모리를 커널로부터 할당받고 있다.
- `openat(...)`: 프로그램 실행에 필요한 공유 라이브러리 파일들을 열고 있다.
- `write(1, ...)`: `ls`가 현재 디렉토리의 파일 목록을 표준 출력(파일 디스크립터 1번)에 출력하고 있다. `write` 시스템 콜을 쓴다.
- `close(1)`: 표준 출력을 닫는다.
- `exit_group(0)`: 프로그램이 정상 종료됐음을 알리며 `exit` 시스템 콜을 호출한다.

이렇게 `strace`를 쓰면 우리가 무심코 사용하던 명령어 이면에서 얼마나 많은 시스템 콜이 커널과 소통하며 일하고 있는지 직접 볼 수 있다. 디버깅하거나 프로그램 동작을 깊이 이해하고 싶을 때 정말 유용한 도구다.

---

시스템 콜은 운영체제의 가장 기본적인 구성 요소 중 하나로, 응용 프로그램이 커널의 보호된 영역에 안전하게 접근할 수 있도록 해주는 다리 역할을 한다. 만약 이런 통제된 인터페이스가 없다면 프로그램들이 하드웨어를 마음대로 건드리거나 다른 프로세스 메모리를 침범해서 시스템 전체가 순식간에 망가질 수도 있을 것이다.

결국 유저 모드와 커널 모드의 분리, 그리고 그 사이를 잇는 시스템 콜이라는 엄격한 절차 덕분에 우리가 안정적인 멀티태스킹 환경을 누리는거다. 지금까지 배운 커널의 개념들이 시스템 콜을 통해 어떻게 실체화되는지 이해할 수 있었다.

### 이후 학습 방향

시스템 콜을 공부하면서 가장 많이 나온 단어는 단연 `프로세스`였다. `fork()`로 프로세스를 만들고, `execve()`로 다른 프로그램을 실행하고, `getpid()`로 ID를 얻는 등 모든게 프로세스 중심으로 이루어진다. 커널이 관리하는 가장 중요한 자원 중 하나인 프로세스가 뭔지, 어떻게 생성되고 관리되는지에 대해 다음번에 깊이 파봐야겠다.
