# SIGHUP은 왜 특별한가?

`SIGHUP`의 `HUP`는 **Hang Up**, 즉 '전화를 끊다'라는 뜻이다. 아주 오래전, 사용자들이 모뎀으로 전화선을 통해 유닉스 시스템에 접속하던 시절의 시그널이다. 사용자가 터미널 접속을 끊으면 즉 전화가 끊기면, 커널은 해당 터미널(세션)에 속한 모든 프로세스에게 "이제 정리하라"는 의미로 `SIGHUP` 시그널을 보냈다. 이 시그널을 받은 프로세스의 기본 동작은 '종료'였다.

이건 프로세스를 공부할 때 확인한 **Session**의 개념과 연결된다. 사용자가 로그아웃하면 세션이 종료되고, 세션에 묶여있던 모든 프로세스 그룹에 `SIGHUP`이 전달되어 일괄적으로 정리된다.

### Reload

그러나 웹서버나 데이터베이스 같은 **데몬** 프로세스들은 사용자가 로그아웃해도 계속 살아있어야 한다. 이 친구들은 세션에 종속되지 않도록 만들어졌기 때문에 `SIGHUP`의 본래 의미는 퇴색되었다.

선배 개발자들은 여기서 잔머리를 발동한다. 어차피 데몬들은 `SIGHUP`을 받아도 죽으면 안 되니, 이 시그널을 무시하는 대신 **특별한 의미를 부여**하기로 했다. 바로 "죽지는 말고, 하던 일은 계속하면서 **설정 파일만 새로 읽어와서 적용해**"라는 컨벤션을 정했다.

이게 `SIGHUP`이 특별한 이유다. '종료'라는 의미의 시그널을 '무중단 리로드'라는 의미로 재해석해서 사용하게 됬다.

특히 클라우드 환경에서 '무중단 리로드' 기능은 매우 중요하다. 무중단 배포 때문에

#### 웹서버의 무중단 설정 변경

다음은 가장 대표적인 예시다. 클라우드에서 운영중인 웹서버에 새로운 도메인을 추가하거나 SSL 인증서를 갱신했다고 가정하면

```bash
# /etc/nginx/nginx.conf 파일 수정 후...

# nginx 대장 프로세스에 SIGHUP 시그널을 보낸다.
kill -HUP $(cat /var/run/nginx.pid)
# 또는
nginx -s reload
```

이 명령을 실행하면 `nginx` 마스터 프로세스는 `SIGHUP`을 받고 죽는 대신, 새로운 설정 파일을 읽어온다. 그리고 기존 연결은 이전 설정을 사용하던 워커 프로세스가 계속 처리하도록 놔두고, 새로 들어오는 연결부터는 새 설정을 적용한 신규 워커 프로세스가 처리하도록 부드럽게 교체한다. 덕분에 **서비스 중단 없이** 설정을 변경할 수 있다.

### 이후 학습 방향

데몬 프로세스(사실 모든 프로세스)를 관리하고, 시그널을 보내는 주체는 결국 운영체제, 즉 커널이다. 커널은 수백 개의 프로세스가 동시에 실행되는 것처럼 보이게 만들기 위해 아주 짧은 시간 단위로 CPU를 어떤 프로세스에 할당할지 결정해야 한다. 이걸 컨텍스트 스위칭이라고 하고 이걸 스케줄러라는 친구가 한다고 한다. 내일은 이 스케줄러가 어떻게 동작하는지 알아봐야겠다.
