### 시그널이란?

프로세스의 Status에서 프로세스가 특정 이벤트가 발생하기를 기다리며 '대기(Waiting)' 상태에 들어간다고 했는데, 그 중요한 이벤트 중 하나가 바로 '시그널'이다. 사용자가 터미널에서 `Ctrl+C`를 누르면 어떻게 실행중이던 프로그램을 멈추는거지? `kill` 명령어는 어떻게 다른 프로세스를 종료시키는거지?

시그널은 유닉스 계열 운영체제에서 **프로세스에 특정 이벤트가 발생했음을 알리는 비동기적인 알림 메커니즘**이다. 하드웨어의 '인터럽트'가 커널에게 무언가 알리는 방식이라면, 시그널은 커널이나 다른 프로세스가 특정 프로세스에게 무언가를 알리는 '소프트웨어 인터럽트'.

어떤 프로세스가 시그널을 받으면 하던 일을 잠시 멈추고 해당 시그널에 대해 약속된 행동을 해야 한다.

### 시그널 처리 방식

프로세스는 시그널을 받았을 때 다음 세 가지 중 하나의 행동을 할 수 있다.

1.  **Default Action**: 모든 시그널에는 커널이 정해놓은 기본 행동이 있다. 예를 들어 `SIGTERM`의 기본 행동은 '프로세스 종료'다. 특별한 설정을 하지 않으면 이 디폴트 행동이 실행된다.
2.  **Ignore the Signal**: 아무것도 하지 않고 무시
3.  **Call a Handler**: 미리 '이 시그널을 받으면 이 함수를 실행해'라고 등록해놓은 특정 시그널 핸들러를 호출한다. 예를 들어 종료 시그널(`SIGTERM`)을 받았을 때 무작정 죽는게 아니라, 작업하던 내용을 파일에 저장하고 종료하는 등의 'Graceful Shutdown' 로직을 시그널 핸들러에 직접 구현할 수 있다.

모든 시그널을 마음대로 수정할 수 있는건 아니다. `SIGKILL`, `SIGSTOP` 시그널은 절대로 무시하거나 핸들러로 잡을 수 없다.

### 주요 시그널의 종류와 kill 명령어

터미널에서 `kill -l` 명령을 실행하면 사용할 수 있는 모든 시그널의 목록을 볼 수 있다.

- **`SIGHUP` (1)**: Hang Up. 터미널 연결이 끊어졌을 때 해당 터미널에서 실행된 프로세스들에게 보내진다. 요즘은 주로 데몬 프로세스에게 '설정 파일을 다시 읽어라'라는 용도로 많이 쓰인다.
- **`SIGINT` (2)**: Interrupt. 사용자가 터미널에서 `Ctrl+C`를 눌렀을 때 보내지는 시그널이다. 보통 '현재 작업을 중단하라'는 의미로 쓰인다.
- **`SIGQUIT` (3)**: Quit. `Ctrl+\`를 눌렀을 때 보내지며, 종료와 함께 코어 덤프(메모리 상태를 파일로 남김. 중요!!)를 생성한다.
- **`SIGKILL` (9)**: Kill. 가장 강력한 종료 시그널. 프로세스는 이 시그널을 거부할 수 없으며, 어떤 정리 작업도 할 기회 없이 즉시 종료된다.
- **`SIGTERM` (15)**: Terminate. 가장 일반적인 종료 시그널. `kill` 명령어가 PID만으로 호출될 때 기본적으로 보내는 시그널이다. `SIGKILL`과 달리 핸들러를 통해 잡을 수 있어서, 프로세스가 종료되기 전에 깔끔하게 정리할 시간을 벌 수 있다.
- **`SIGCHLD` (17)**: Child. 자식 프로세스가 종료되거나 멈췄을 때 부모 프로세스에게 보내진다. 이 시그널을 처리해줘야 좀비 프로세스가 생기는걸 막을 수 있다.
- **`SIGSTOP` / `SIGCONT`**: 프로세스를 일시 정지(`Ctrl+Z`)시키거나, 멈춘 프로세스를 다시 실행시키는 시그널이다.

`kill` 명령어는 이름과 달리 단순히 프로세스를 죽이는게 아니라, **'프로세스에 특정 시그널을 보내는'** 명령어다.

### 이후 학습 방향

시그널 중 `SIGHUP`은 주로 데몬의 설정을 리로드하는 데 쓰인다고 한다. 내일은 데몬 프로세스와 현대에서 DevOps 관점의 `SIGHUP` 시그널의 관계에 대해 확인해봐야겠다.
