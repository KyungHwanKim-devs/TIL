# Libuv란 무엇인가?

지난번 NodeJs를 정리하며 계속 언급되었던 **libuv**에 대해 한 단계만 깊이 파고들어 보려고 한다.
NodeJs의 '논블로킹 I/O와 이벤트 루프'를 검색하면 항상 나오는게 'libuv'였는데, 그동안은 막연히
"백그라운드에서 비동기를 위해 뭔가 해주는 친구" 정도로만 알고 있었는데, 이번 기회에 제대로 정리해보려 한다.

일단 **libuv**를 한 줄로 요약하자면 **NodeJs 안에서 I/O 관련 비동기 역할을 수행해주는 C++로 작성된 `크로스 플랫폼` `비동기 I/O` 라이브러리**다.
즉 NodeJs 환경에서 자바스크립트 자체는 V8 엔진에서 싱글스레드로 실행되지만, 파일 시스템 접근, 네트워크 통신, 타이머 등의 I/O 혹은 시간이 걸리는 거의 모든 작업은 libuv에게 떠넘기는 식으로 작동한다.

### libuv의 탄생 배경과 역사

libuv를 만든 이유는 기존의 I/O 라이브러리들이 가진 한계 때문이었다

크게는 **select/poll**에서 오는 동기 방식의 한계와, Linux/Unix에서만 동작하는 **libev**, 혹은 윈도우 에서만 작동하는 **IOCP** 등 크로스 플랫폼간 일관성 부족에 한계가 있었다.

### libuv는 이 문제들을 어떻게 해결했는가?

libuv는 이러한 문제들을 해결하기 위해 **추상화 계층(Abstraction Layer)**을 제공한다. 즉, 각 운영체제가 제공하는 가장 효율적인 비동기 I/O 모델을 내부적으로 사용하면서, 개발자에게는 일관된 API를 제공하는 것이다.

- **Windows**: IOCP (I/O Completion Port) 사용
- **Linux**: epoll 사용
- **macOS, FreeBSD**: kqueue 사용

이 덕분에 Node.js 개발자들은 운영체제별 내부 구현을 신경 쓸 필요 없이, libuv가 제공하는 API만으로 어떤 환경에서든 동일하게 동작하는 비동기 코드를 작성할 수 있게 되었다.

### libuv의 핵심 구조: 이벤트 루프와 스레드 풀

libuv의 아키텍처는 크게 두 가지 핵심 요소로 구성된다. 바로 **이벤트 루프(Event Loop)**와 **워커 스레드 풀(Worker Thread Pool)**이다.

**1. 이벤트 루프 (Event Loop)**
이벤트 루프는 libuv의 심장과도 같다. 단일 스레드로 실행되며, I/O 작업이 완료되기를 기다렸다가, 완료되면 해당 작업에 연결된 콜백 함수를 실행하는 역할을 한다. 네트워크 요청과 같은 대부분의 비동기 작업은 이 이벤트 루프를 통해 효율적으로 처리된다. OS 커널의 도움을 받아 논블로킹 방식으로 작동하기 때문에 하나의 스레드로도 수많은 동시 요청을 처리할 수 있다.

**2. 워커 스레드 풀 (Worker Thread Pool)**
하지만 모든 작업이 OS 수준에서 비동기로 처리될 수 있는 것은 아니다. 파일 시스템 접근(Disk I/O)이나 DNS 조회, 암호화와 같이 일부 작업들은 본질적으로 블로킹(동기) 방식으로 동작할 수밖에 없다.

만약 이런 작업들을 이벤트 루프에서 직접 처리한다면, 작업이 끝날 때까지 전체 애플리케이션이 멈추게 될 것이다. 이를 방지하기 위해 libuv는 내부에 여러 개의 **워커 스레드**를 가진 **스레드 풀**을 유지한다. 블로킹이 필요한 작업이 요청되면, libuv는 해당 작업을 스레드 풀의 워커 스레드에게 위임한다. 워커 스레드가 백그라운드에서 작업을 처리하는 동안 이벤트 루프는 다른 요청들을 계속해서 처리할 수 있다. 작업이 완료되면 워커 스레드는 이벤트 루프에게 알리고, 이벤트 루프는 준비된 콜백을 실행한다.

### Node.js와 libuv의 상호작용

우리가 Node.js에서 `fs.readFile()` 같은 비동기 함수를 호출하면 다음과 같은 과정이 일어난다.

1.  JavaScript 코드가 V8 엔진에 의해 실행된다.
2.  `fs.readFile()` 호출은 Node.js의 C++ 바인딩을 통해 libuv에게 전달된다.
3.  `readFile`은 블로킹 I/O 작업이므로, libuv는 이 요청을 **스레드 풀**의 워커 스레드 중 하나에 할당한다.
4.  워커 스레드가 백그라운드에서 파일을 읽는 동안, **이벤트 루프**는 멈추지 않고 다른 JavaScript 코드를 계속 실행하거나 다른 이벤트를 처리한다.
5.  파일 읽기가 완료되면, 워커 스레드는 그 결과를 이벤트 루프에 알린다.
6.  이벤트 루프는 완료된 작업의 콜백 함수를 받아 **콜백 큐**에 넣고, 적절한 시점에 V8 엔진이 이 콜백을 실행하여 결과를 처리한다.

### 이벤트 루프의 상세 동작 단계

앞서 이벤트 루프를 libuv의 심장이라고 표현했는데, 사실 이 심장은 매우 정교한 주기에 맞춰 박동한다. 이벤트 루프는 단순히 '작업이 끝나면 콜백을 실행'하는 것 이상으로, 여러 단계(Phase)를 순서대로 순회하며 각 단계에 맞는 콜백들을 처리한다. Node.js 공식 문서에 따르면 주요 단계는 다음과 같다.

1.  **Timers**: `setTimeout()`, `setInterval()`로 스케줄링된 타이머 콜백을 실행한다.
2.  **Pending Callbacks**: 지난번 루프에서 완료되지 않았던 I/O 작업의 콜백들을 실행한다.
3.  **Idle, Prepare**: 내부용으로만 사용된다.
4.  **Poll**: 새로운 I/O 이벤트를 가져오고, 관련된 콜백(네트워크 통신, 파일 읽기 등)을 실행한다. 이 단계에서 필요하다면 다음 이벤트가 생길 때까지 블로킹 상태에 들어간다.
5.  **Check**: `setImmediate()`로 스케줄링된 콜백을 실행한다.
6.  **Close Callbacks**: 소켓이나 핸들이 닫힐 때 발생하는 `close` 이벤트의 콜백을 실행한다.

이러한 단계들이 계속해서 순환하기 때문에 '이벤트 루프'라고 부르는 것이다. `setImmediate()`가 `setTimeout(fn, 0)`보다 먼저 실행될 수도 있는 이유가 바로 이 루프의 특정 단계에서 각 콜백이 실행되기 때문이다.

### libuv의 또 다른 핵심: 핸들(Handles)과 요청(Requests)

libuv를 좀 더 깊이 이해하려면 핸들과 요청이라는 두 가지 개념을 알아야 한다.

- **핸들 (Handles)**: 활성화되어 있는 동안 특정 작업을 수행할 수 있는 오래 지속되는 객체를 나타낸다. 예를 들어 TCP 서버(`net.Server`), 타이머, 또는 표준 입출력 스트림 등이 핸들에 해당한다. 이들은 '살아있는' 동안 계속해서 이벤트를 발생시킬 수 있다.
- **요청 (Requests)**: 핸들을 통해 수행되는 단기적인(short-lived) 작업을 나타낸다. 예를 들어 `fs.writeFile()`을 호출하면, 이 작업은 libuv 내에서 '쓰기 요청' 객체로 표현된다. 작업이 완료되면 요청 객체와 연결된 콜백이 실행되고, 그 후 객체는 소멸한다.

쉽게 비유하자면, 핸들은 '전화기'이고 요청은 그 전화기로 거는 '한 통의 전화'라고 볼 수 있다. 전화기는 계속 존재하지만(long-lived), 전화 한 통은 목적을 달성하면 끊어지는(short-lived) 것과 같다.

### 결론

libuv는 단순히 Node.js의 부속품이 아니라, Node.js의 비동기, 논블로킹 특성을 가능하게 하는 핵심 기반이다. 운영체제별로 파편화된 비동기 I/O 모델을 추상화하고, 이벤트 루프와 스레드 풀의 유기적인 조합을 통해 단일 스레드인 JavaScript가 높은 동시성과 성능을 가질 수 있도록 만들어준다. 결국 Node.js의 작동 원리를 깊이 이해하기 위해서는 libuv에 대한 이해가 필수적이라고 할 수 있겠다.

## 다음 공부 계획

커널 공부 때와 마찬가지로, libuv 역시 파고들수록 새로운 개념들이 계속해서 나온다. 이벤트 루프가 구체적인 단계들로 나뉘어 있다는 점, 그리고 핸들과 요청이라는 개념이 내부적으로 쓰인다는 점이 흥미로웠다. 다음에는 실제로 Node.js 코드를 작성하면서 `process.nextTick()`, `setImmediate()`, `setTimeout()` 등이 이벤트 루프의 어떤 단계에서 어떻게 실행되는지 직접 눈으로 확인하며 더 깊이 이해해봐야겠다.
