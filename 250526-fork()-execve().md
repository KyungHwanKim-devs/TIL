# fork()와 execve()

저번에 프로세스가 뭔지, 커널이 어떻게 관리하는지 알아봤다. 이번엔 이 프로세스가 도대체 어떻게 만들어지는건지 알아봐야겠다. 쉘에 `ls` 같은 명령어를 치면 새로운 프로세스가 생기는데, 그 배경에는 바로 유닉스 계열 시스템의 메커니즘인 `fork()`와 `execve()` 시스템 콜이 있다.

### fork() 시스템 콜

`fork()`는 특이한 시스템 콜이다. 하는 일은 딱 하나, **자기 자신과 거의!! 모든게 똑같은 새로운 프로세스(자식 프로세스)를 만드는 것**이다. 호출하는 순간, 메모리 내용, 열려 있는 파일 목록 등 모든게 복제된 프로세스가 하나 더 생긴다

`fork()` 호출이 끝나면 두 개의 프로세스가 존재하게 되는데, 신기한 건 이 둘을 코드상에서 구분할 수 있다는 거다. 리턴도 두개로 낼수있다??

- **부모 프로세스**: `fork()`는 자식 프로세스의 PID(양수)를 반환한다.
- **자식 프로세스**: `fork()`는 `0`을 반환한다.

이 반환값의 차이를 이용해서 개발자는 부모와 자식이 각기 다른 작업을 수행하도록 코드를 작성할 수 있다.
다음은 유명한 fork()의 코드 예시이다.

```c
#include <stdio.h>
#include <unistd.h>

int main() {
    pid_t pid = fork();

    if (pid > 0) {
        // 부모 프로세스의 코드
        printf("부모: 내 PID는 %d, 내 자식의 PID는 %d야.\n", getpid(), pid);
    } else if (pid == 0) {
        // 자식 프로세스의 코드
        printf("자식: 내 PID는 %d, 내 부모의 PID는 %d야.\n", getpid(), getppid());
    } else {
        // fork 실패
        perror("fork");
    }

    return 0;
}
```

여기서 중요한 최적화 기법으로 **Copy-on-Write (COW)**가 있다. `fork()`를 호출하자마자 부모의 모든 메모리를 물리적으로 전부 복사하는건 비효율적이다. 그래서 커널은 일단 메모리 공간을 공유하게 놔두다가, 부모나 자식 중 어느 한쪽이 메모리에 무언가 쓰려고(Write) 할 때 그제서야 해당 페이지만 복사(Copy)해서 분리한다.

### execve() 시스템 콜

`execve()`가 호출되면 현재 프로세스의 메모리 공간(코드, 데이터, 스택 등)을 싹 날리고 그 자리에 새로운 프로그램의 내용이 올라온다. 중요한 건 **새로운 프로세스가 생성되는게 아니라는 점**이다. PID는 그대로 유지되며, 오직 내용물만 바뀐다. 한번 `execve()`를 호출하면 원래 프로그램으로는 다시 돌아올 수 없다.

### fork()와 execve()의 조합

유닉스 시스템에서 새로운 프로그램을 실행하는 과정은 이 두 시스템 콜의 조합으로 이루어진다. 쉘이 `ls -l` 명령을 실행하는 과정을 따라가 보자.

1.  현재 실행중인 쉘(`bash`, `zsh` 등)이 `fork()`를 호출해서 자신의 복제본인 자식 프로세스를 만든다.
2.  이제 두 개의 쉘 프로세스가 있다. 부모(원래 쉘)와 자식(복제된 쉘).
3.  **자식 프로세스**는 `fork()`의 반환값이 `0`인걸 확인하고, `execve()` 시스템 콜을 호출한다. 이때 인자로 실행할 프로그램의 경로(`/bin/ls`)와 옵션(`-l`)을 넘겨준다.
4.  자식 프로세스는 `ls` 프로그램으로 완전히 변신해서 `ls -l`의 실행 결과를 화면에 출력하고 생을 마감한다.
5.  한편, **부모 프로세스**는 `fork()`로부터 자식의 PID를 반환받았기 때문에, 보통 `wait()` 시스템 콜을 호출해서 자식 프로세스가 끝날 때까지 기다린다.
6.  자식 프로세스가 종료되면 `wait()`이 반환되고, 부모 프로세스는 다시 프롬프트를 띄우며 다음 명령을 기다린다.

---

이렇게 '복제하고, 바꿔치기'하는 `fork()`와 `execve()`의 2단계 방식은 매우 단순하면서도 강력하다. 이 덕분에 쉘은 자식 프로세스가 어떤 프로그램을 실행하든 상관없이 자신의 상태를 안전하게 유지할 수 있고, 파이프(`|`)나 리다이렉션(`>`) 같은 복잡한 기능도 이 기본 메커니즘 위에서 쉽게 구현될 수 있다.

### 이후 학습 방향

프로세스가 어떻게 생성되고 다른 프로그램으로 바뀌는지 알았다. 이제는 프로세스의 '상태'에 대해 좀 더 깊이 들어가 볼 차례다. 프로세스가 실행되고, 잠자고, 다시 깨어나는 등 그 생명 주기? 프로세스의 여러 상태 Process Status 와 상태 전이에 대해 알아봐야겠다.
