# 커널 관련 개념들

저번에 알아본 바로 좁은 의미의 리눅스는 커널 그 자체라고 했다. 그리고 그 커널이 주로 하는일도 알아봤다.
크게는 프로세스 관리, 메모리 관리, 파일 시스템, 네트워킹, 장치 드라이버가 있는데 이것들을 파보기전에 알아야될 커널과 관련된 개념들을 먼저 알아보도록 하자.

### 커널의 위치

보통은 부팅 가능한 디스크의 특정 파티션(보통 `/boot` 폴더)에 `vmlinuz`라는 이름의 압축 파일로 저장되어 있다. 커널이 `메모리`에 적재된 후에는 보통 `커널공간` 이라는 보호된 메모리에서 사라지지 않고 항상 상주하면서 시스템 전체를 제어한다.

### 리눅스의 부팅 과정

1. 컴퓨터에 전원이 들어오면 BIOS가 먼저 실행되어 하드웨어를 확인한다.
2. BIOS는 부팅 가능한 저장 장치에서 **부트로더**를 찾아 실행시킨다.
3. 부트로더는 설정 파일을 읽어 사용자가 커널 파일 `vmlinuz`과 `initramfs` 파일을 `메모리`에 올린다.
4. 모든 준비가 끝나면 부트로더는 제어권을 메모리에 올라가있는 커널로 넘긴다. 이 시점부터 커널이 실행되기 시작.
5. 커널은 실행 된 후 `/sbin/init` 또는 설정된 경로에 있는 `init 프로세스`를 찾아 실행한다. 이 프로세스는 항상 1번 `PID`를 부여받으며, 이후 모든 다른 프로세스들의 부모가 된다. (현대 리눅스에서는 이 `init` 프로세스는 대부분 `systemd`).

### 유저모드와 커널모드

먼저 CPU의 모드(Mode)란, CPU가 현재 어떤 권한 수준(Privilege Level)에서 명령어를 실행하고 있는지를 나타내는 상태라고 한다.
공룡책에 두 모드에 대한 명확한 정의는 따로 없어서(생각보다 널리 알려진 키워들에 대한 명확한 정의가 있지는 않더라.....) 일단 위키의 정의를 빌려와 보자.

1. 커널모드(Kernel Mode) : Kernel mode is the privileged mode where the core part of the operating system (the kernel) executes. It has unrestricted access to all machine resources including CPU, memory, storage, and connected devices - 위키

해석 해보자면 커널 모드는 운영체제의 핵심인 커널이 실행되는 `특권` 모드(Ring 0)이다. 이 모드에서는 CPU, 메모리, 저장장치 및 하드웨어 장치 등 모든 시스템 자원에 **무제한** 접근할 수 있다.

2. 유저모드(User Mode) : User mode is a restricted environment where application programs run. Programs in user mode cannot access hardware or kernel memory directly; they must request access through system calls to the kernel - 위키

사용자 모드는 일반 응용 프로그램이 실행되는 `제한된` 환경(Ring 3)이다. 사용자 모드의 프로그램은 하드웨어나 커널 메모리에 직접 접근할 수 없으며, 운영체제 커널에 시스템 콜을 통해 요청해야 한다.

### 유저공간과 커널공간

1. 커널 공간 (Kernel Space) : Kernel space is strictly reserved for running a privileged operating system kernel, kernel extensions, and most device drivers. - 위키

커널 공간은 사용자 공간과 분리되어 있으며, `프로세서`가 사용자 모드일 때는 접근이 금지된다. 이렇게 분리함으로써 사용자 프로그램이 커널 코드를 손상시키거나 시스템 자원을 임의로 사용하는 것을 방지한다. 리눅스 커널은 물리적 메모리 상의 고정된 영역에 상주하면서 `시스템 콜` 처리, `인터럽트` 관리, 메모리 매핑 등 중요한 작업을 수행한다.

1. 유저 공간 (Kernel Space) : User space refers to the memory area where all user mode applications run, isolated from the kernel, with restricted access to system resources. - 위키

유저 공간은 사용자 모드 애플리케이션이 실행되는 메모리 영역으로, 커널로부터 분리되어 있으며 시스템 자원 접근이 제한된다. 필요한 경우에만 `시스템 콜`을 통해서만 자원에 접근한다. 이렇게 분리된 환경은 응용 프로그램이 오류를 일으켜도 커널과 다른 프로세스에 영향을 주지 않게 된다.

### 커널의 종류

운영체제 커널은 설계 방식에 따라 크게 모놀리식 커널과 마이크로커널로 나눌 수 있다.

**1. 모놀리식 커널 (Monolithic Kernel)**
모놀리식 커널은 운영체제의 핵심 기능(프로세스 관리, 메모리 관리, 파일 시스템, 장치 드라이버 등)이 모두 하나의 큰 프로그램으로 통합되어 커널 공간에서 실행되는 구조이다. 리눅스는 대표적인 모놀리식 커널이다.

- **장점**: 커널 내부의 기능들이 서로 직접 통신하므로 성능이 매우 빠르다.
- **단점**: 하나의 버그가 시스템 전체에 영향을 줄 수 있어 안정성이 낮고, 새로운 기능을 추가하거나 수정하기가 어렵다.

**2. 마이크로커널 (Microkernel)**
마이크로커널은 커널의 기능을 최소화하여 가장 필수적인 부분(프로세스 간 통신, 기본적인 메모리 관리 등)만 남기고, 나머지 기능들은 유저 공간에서 별도의 서버 프로세스로 실행하는 구조이다.

- **장점**: 각 기능이 모듈화되어 있어 안정성이 높고, 새로운 기능을 추가하거나 수정하기 용이하다.
- **단점**: 기능 간 통신이 커널을 거쳐야 하므로(IPC, Inter-Process Communication), 모놀리식 커널에 비해 성능이 느릴 수 있다.

### 인터럽트 (Interrupts)

인터럽트는 CPU가 프로그램을 실행하고 있을 때, 입출력 하드웨어나 예외상황 등이 발생하여 처리가 필요할 경우에 CPU에게 알려 처리할 수 있도록 하는 것을 말한다. 인터럽트가 발생하면 CPU는 현재 실행 중이던 작업을 잠시 멈추고, 해당 인터럽트를 처리하기 위한 `인터럽트 서비스 루틴(ISR)`을 실행한다. 처리가 완료되면 다시 원래 작업으로 복귀한다.

- **하드웨어 인터럽트**: 키보드 입력, 마우스 움직임, 디스크 작업 완료 등 하드웨어 장치가 CPU의 서비스가 필요할 때 발생시킨다.
- **소프트웨어 인터럽트**: 프로그램이 특별한 처리를 요구할 때, 예를 들어 시스템 콜을 호출하거나, 0으로 나누는 등의 예외 상황이 발생했을 때 일어난다. `시스템 콜`은 일종의 의도적인 소프트웨어 인터럽트라고 볼 수 있다.

커널은 이러한 인터럽트를 관리하고 적절히 대응함으로써 시스템의 여러 장치와 프로그램들이 조화롭게 동작하도록 제어한다.

### 컨텍스트 스위칭 (Context Switching)

컨텍스트 스위칭은 하나의 프로세스가 CPU 사용을 중단하고, 다른 프로세스가 CPU를 사용할 수 있도록 `실행 환경을 교체`하는 과정을 의미한다. 멀티태스킹 운영체제에서 여러 프로세스가 동시에 실행되는 것처럼 보이게 하는 핵심 기술이다.

이 과정에서 커널은 현재 실행 중인 프로세스의 상태(레지스터 값, 프로그램 카운터 등)를 PCB(Process Control Block)에 저장하고, 새로 실행할 프로세스의 상태를 PCB에서 불러와 CPU에 적재한다. 이 작업은 CPU 시간을 소모하므로, 컨텍스트 스위칭이 너무 잦아지면 시스템 전체의 성능 저하(Overhead)를 유발할 수 있다.

### 결론

지금까지 커널의 기본적인 개념과 역할, 그리고 어떻게 시스템이 사용자 요청을 안전하게 처리하는지에 대해 알아보았다. 커널은 운영체제의 심장과도 같아서, 하드웨어라는 물리적 세계와 우리가 사용하는 소프트웨어 애플리케이션 사이를 중재하는 핵심적인 역할을 담당한다. 유저 모드와 커널 모드, 그리고 각자의 메모리 공간을 분리하는 것은 시스템의 안정성과 보안을 지키는 매우 중요한 설계 원칙이다. 이러한 기반 지식을 바탕으로 다음 단계에서는 시스템 콜에 대해 더 깊이 파고들며 운영체제의 작동 원리를 계속 탐험해 나갈 것이다.

## 다음 공부 계획

커널을 깊이 공부한것도 아니고 개념 정리만 하는데 또 처음 보는 키워드들이 막 튀어 나온다. 이 주변 키워드들에 대해서 학습하지 않으면 더이상 깊이 내려갈 수 없을것 같다.
무슨 고구마줄기도 아니고 공부를 하면 할수록 공부를 해야할 키워드나 개념이 늘어나는 기분이다........
내일은 NodeJs에서부터 오늘까지 줄창 언급되는 `시스템콜`에 대해서 알아봐야겠다.
