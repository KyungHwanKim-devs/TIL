# 선언형 vs 명령형 in UI

지난번 리액트를 정리하며 중요하다고 느꼈던 '선언형'이라는 키워드에 대해 한 칸만 더 파고들어 보자. 처음에는 단순히 코드를 쓰는 스타일의 차이라고 생각했는데, 찾다보니 코드작성을 바라보는 근본적인 관점의 차이 즉 패러다임의 차이라는 것을 느꼇다.

---

### 명령형 프로그래밍

**명령형 프로그래밍**은 코드를 작성 할 때 **'어떻게(How)'** 해야 하는지에 초점을 맞춘다. 작업의 순서, 분기, 반복문 등 제어를 명시적으로 작성하고 상태를 직접 변경하는 방식으로 코드를 작성한다.

예를 들어 "숫자로 된 배열의 모든 요소를 2배로 만들어"라는 목표가 있다면, `for` 문을 사용해 직접 순회하고, 계산하고, 새로운 배열에 값을 넣는 모든 과정을 명시적으로 제어한다.

```javascript
const numbers = [1, 2, 3, 4];
const doubled = [];

// "어떻게" 행동 할지 하나하나 명령하듯이 작성한다.
for (let i = 0; i < numbers.length; i++) {
  const currentValue = numbers[i];
  const newValue = currentValue * 2;
  doubled.push(newValue);
}

console.log(doubled);
// [2, 4, 6, 8]
```

---

### 선언형 프로그래밍

**선언형 프로그래밍**은 결국 코드로 **'무엇을(What)'** 원하는지에 초점을 맞춘다. 목표를 달성하기 위한 상세한 과정이나 절차는 숨겨져 있으며, 개발자는 원하는 결과가 무엇인지만을 보이게 작성한다.

같은 목표를 `map`이라는 고차 함수를 사용해 구현해보자. "배열의 각 요소에 `double` 함수를 적용한 새로운 배열을 원해"라고 **선언**할 뿐, 순회나 값 할당 과정은 `map` 함수 내부에 숨겨져 있다.

```javascript
const numbers = [1, 2, 3, 4];

function double(number) {
  return number * 2;
}

// "무엇을" 원하는지 선언한다.
const doubled = numbers.map(double);

console.log(doubled);
// [2, 4, 6, 8]
```

---

두 작성 방식의 차이는 데이터 통신이 포함된 UI를 개발할 때 극명하게 갈린다. "유저의 프로필을 백엔드에 요청해서 화면에 표시"하는 기능을 두 방식으로 구현해 보자.

#### jQuery (명령형 방식)

jQuery는 AJAX 호출의 각 단계(시작, 성공, 실패)마다 DOM 요소를 **직접 찾아** 데이터를 **교체하라고 일일이 명령**한다. 로딩중, 성공, 에러 상태를 관리하는 로직이 콜백 함수 안에 섞여 코드가 복잡해지기 쉽다.

```html
<div id="profile"></div>
<button id="load-button">프로필 불러오기</button>
```

```javascript
$("#load-button").on("click", function () {
  $.ajax({
    url: "/api/user/1",
    beforeSend: function () {
      $("#profile").html("<p>로딩 중...</p>");
    },
  })
    .done(function (user) {
      $("#profile").html(`<h2>이름 : ${user.name}</h2>`);
    })
    .fail(function (error) {
      $("#profile").html(`<p>오류: ${error.statusText}</p>`);
    });
});
```

#### React (선언형 방식)

리액트는 **서스펜스**와 **에러 바운더리**를 통해 비동기 상태를 선언적으로 다룰 수 있다. 개발자는 로딩, 성공, 에러 각 상태에 **무엇을(What)** 보여줄지만 선언하면, 리액트가 상황에 맞는 UI를 알아서 렌더한다.

```javascript
function Profile() {
  const user = useUserData("/api/user/1");
  return <h2>{user.name}</h2>;
}

function App() {
  return (
    <div>
      <h1>내 프로필</h1>
      <ErrorBoundary fallback={<p>오류 발생.</p>}>
        <Suspense fallback={<p>로딩 중...</p>}>
          <Profile />
        </Suspense>
      </ErrorBoundary>
    </div>
  );
}
```

jQuery 코드는 "언제, 무엇을, 어떻게 바꿔라"는 식의 절차를 명령하듯이 나열하는 반면, React 코드는 각 상태에 따른 최종 결과물만 선언하면 된다. 덕분에 비즈니스 로직과 상태 관리 로직이 분리되어 훨씬 직관적고 유지보수가 편한 코드를 작성할 수 있다.

---

사실 일을 하는 동안에는 jQuery를 직접 써본적은 없었다. 그러나 커리어 초반애는 리액트로 코드를 작성할때도 위에서 작성한 명령형 코드 처럼 작성하는 경우도 많았다. 하지만 그렇게 작성해보니 추후에 기능이 추가되거나 수정등을 해야할 때 점점 더 코드가 복잡해져서(중첩 삼항연산자 등...) 나중애는 막연하게 랜더에 대한 책임을 컴포넌트 스스로 가져가도록 작성하기 시작했고 이게 곧 선언적 컴포넌트를 작성하는 시작이 되었었다.
결국 내가 느꼇던건 명령형 코드는 상태 관리를 위한 변수와 콜백이 얽히기 시작하면 복잡도가 급격히 올라가는 느낌이었다.

반면 리액트의 선언형 방식은 함수형 컴포넌트와 `Suspense`, `ErrorBoundary`등을 통해 로딩과 에러라는 복잡한 상태까지도 간단하게 표현할 수 있게 해줬다. 이 작성 방식은 로딩과 에러까지도 하나의 예측 가능한 상태로 다루게 해주는 강력한 패러다임이라고 생각한다.
