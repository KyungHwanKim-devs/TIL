### 프로세스의 한계

프로세스는 운영체제가 관리하는 태스크 단위다. 그리고 각 프로세스는 독립적인 메모리 공간을 가진다. 이 방법은 안전할 수 있지만 또한 비효율적일 수 있다.

예를 들어 웹서버를 만든다고 해보자. 사용자 요청이 오면 새로운 프로세스를 만들어서 처리하는 방식 이라면

```
요청 1 -> 프로세스 A (메모리 50MB)
요청 2 -> 프로세스 B (메모리 50MB)
요청 3 -> 프로세스 C (메모리 50MB)
```

단순계산으로 동시 접속자 1000명이면? 프로세스 1000개 = 50GB 메모리를 쓰게된다. 게다가 프로세스 생성(`fork()`)은 느리다. 메모리를 복사하고(COW지만), PCB를 만들고, 페이지 테이블을 복사하는 등의 작업을 수반하기 때문에 오버헤드가 아주 크다.

또 다른 문제는 **프로세스 간 통신(IPC, Inter-Process Communication)**이다. 프로세스끼리 데이터를 주고받으려면 파이프, 소켓, 공유 메모리 같은 복잡한 메커니즘이 필요하다.

### 그래서 등장하는 스레드의 개념

**스레드(Thread)**는 리눅스에서는 "경량 프로세스(Lightweight Process)"라고도 불린다.

**같은 프로세스의 스레드들은 메모리를 공유한다**.

```
프로세스 A
  ├─ 스레드 1 (Stack만 독립)
  ├─ 스레드 2 (Stack만 독립)
  └─ 스레드 3 (Stack만 독립)

공유:
  - Code 영역
  - Data 영역
  - Heap 영역
  - 열린 파일 등
```

### 프로세스 vs 스레드 비교

| 특징            | 프로세스                  | 스레드                    |
| --------------- | ------------------------- | ------------------------- |
| 메모리 공간     | 독립적                    | 공유 (Stack만 독립)       |
| 생성 속도       | 느림 (`fork()`)           | 빠름 (`pthread_create()`) |
| 컨텍스트 스위칭 | 느림 (페이지 테이블 교체) | 빠름 (레지스터만 교체)    |
| 통신            | 복잡 (IPC 필요)           | 쉬움 (변수 공유)          |
| 안전성          | 높음 (격리됨)             | 낮음 (공유 메모리 경쟁)   |
| 메모리 사용     | 많음                      | 적음                      |

### 다음의 경우 스레드를 사용하는게 효율적일 수 있다

1. **동시성이 필요한 서버**: 웹서버, 데이터베이스 등. 스레드 하나당 클라이언트 하나를 담당하면 동시에 여러 요청을 처리할 수 있다.

2. **CPU 집약적 작업 분산**: 멀티코어 CPU를 활용할 수 있다. 이미지 처리, 영상 인코딩 같은 작업을 여러 스레드로 나눠서 병렬 처리.

3. **반응성 유지**: GUI 프로그램에서 무거운 작업을 백그라운드 스레드에서 하면 메인 UI가 멈추지 않는다.

4. **자원 공유**: 같은 데이터를 여러 실행 흐름이 접근해야 하는 경우.

### 스레드의 위험성

메모리를 공유하면 편하지만 위험하다. **Race Condition**이 발생할 수 있다.

리눅스의 경우 커널 입장에서는 **프로세스와 스레드가 거의 같다**. 둘 다 `task_struct`라는 자료구조로 표현된다.

차이는 생성할 때 `clone()` 시스템 콜에 넘기는 플래그라고 한다

- 프로세스 생성: `clone(SIGCHLD)`
- 스레드 생성: `clone(CLONE_VM | CLONE_FS | CLONE_FILES | CLONE_SIGHAND)`

`CLONE_VM`은 "메모리 공유", `CLONE_FILES`는 "파일 디스크립터 공유" 등을 의미한다.

커널 입장에서는 둘 다 스케줄링 대상일 뿐이다. 그래서 리눅스에서는 "Light Weight Process(LWP)"라고 부른다.

### 이후 학습 방향

사실 쓰레드 자체는 크게 어려운 개념이 아닌것 같다. 다만 스레드의 가장 큰 문제인 Race Condition을 해결하려면 **동기화(Synchronization)**가 필요한데 여기서 나오는 개념들이 좀 어렵다. Mutex, Semaphore, Condition Variable, Deadlock 같은 개념들을, 무슨 개념인지, 정확히 어떻게 쓰는지, 왜 필요한지 다음번에 각각 깊이 파봐야겠다.
