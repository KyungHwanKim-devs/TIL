# 리액트란 무엇인가?

사실 프론트엔드 개발자로 일했을 당시에 기술에 대한 이해없이 그저 라이브러리를 '사용' 하기만 했던거 같아서 깊이 있게 공부를 시작해 보려 한다.

**리액트**를 한줄로 요약 하자면 페이스북이 개발한 **UI를 쉽고 빠르게 구현하기 위한 자바스크립트 라이브러리**이다.

최초의 아이디어는 "데이터가 변하면 UI도 그에 맞춰 자동으로 업데이트되어야 한다"는 간단한 아이디어에서 출발했다고 한다.
UI 개발자는 특정 상태에서 UI가 어떻게 보일지만 **선언적**으로 정의하면, 리액트가 DOM 업데이트 등을 통해서 UI를 랜더해준다.

공식문서와 여러 아티클을 읽어본 결과 리액트의 핵심적인 4가지 요소는 **컴포넌트(Component), JSX, 상태(State), VirtualDOM** 이라고 할 수 있을 것 같다.

- **컴포넌트 (Component)**

  - UI를 구성하는 독립적이고 재사용 가능한 최소 단위.
  - 하나의 컴포넌트는 자신 스스로의 기능(hook), HTML 구조(JSX), 스타일(CSS)을 가질 수 있다.
  - 크게 함수형 컴포넌트와 클래스형 컴포넌트가 있으며, 현재는 주로 함수형 컴포넌트와 훅(Hook)을 사용하는 추세다.

```
// 아주 간단한 함수형 컴포넌트와 훅
function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>현재 카운트: {count}</p>
      <button onClick={() => setCount(count + 1)}>+1</button>
    </div>
  );
}

// 클래스형 컴포넌트
class Greeting extends React.Component {
  render() {
    return <h2>안녕하세요, {this.props.name}!</h2>;
  }
}

// 사용
export default function App() {
  return (
    <div>
      <Greeting name="김경환" />
      <Counter />
    </div>
  );
}
```

- **JSX 문법**

  - HTML과 유사한 문법으로 자바스크립트를 함께 작성할 수 있게 해주는 확장 문법이다.
  - 각 컴포넌트(UI)가 어떻게 생겼는지 직관적으로 보여주며, UI와 로직을 하나의 파일에서 쉽게 관리할 수 있게 해준다.
  - 브라우저가 직접 읽을 수는 없고, 바벨(Babel)을 통해 일반 자바스크립트 코드로 변환되는 과정을 거친다.

```
// JSX 문법으로 만든 컴포넌트
function Welcome({ name }) {
  return <h1>안녕하세요, {name}!</h1>;
}

export default function App() {
  const user = "김경환";
  return (
    <div>
      <Welcome name={user} />
      <p>{user}님, 환영합니다 🎉</p>
    </div>
  );
}

// 이제 이걸 바벨같은 트랜스파일러가 다음과 같은 자바스크립트 문법으로 변환해준다.
function Welcome({ name }) {
  return React.createElement("h1", null, "안녕하세요, ", name, "!");
}

export default function App() {
  const user = "김경환";
  return React.createElement(
    "div",
    null,
    React.createElement(Welcome, { name: user }),
    React.createElement("p", null, user, "님, 환영합니다 🎉")
  );
}
```

- **상태(State)**

  - **컴포넌트** 내부에서 관리되는 동적인 데이터. 사용자의 입력이나 이벤트, 서버 응답 등 컴포넌트 내부에서 변할 수 있는 값을 다룬다. State가 변경되면 컴포넌트가 다시 렌더링된다.

- **Virtual DOM**
  - 성능면에서 이게 리액트의 핵심이라고 할 수 있을 것 같다. 실제 자바스크립트의 DOM을 직접 조작하는 것보다 가상의 DOM을 메모리에 만들어서 변경사항을 비교한 다음에 필요한 부분만 실제 DOM에 반영하는 방식이다.

리액트에서는 이 요소들이 유기적으로 연결되서 작동한다.

1.  **데이터 흐름**: 데이터는 기본적으로 부모에서 자식으로, 즉 **Props**를 통해 위에서 아래로 흐르는 **단방향 데이터 흐름**이다.

2.  **상태 변경과 리렌더링**: 사용자가 버튼을 클릭하는 등의 상호작용이 발생하면, 해당 컴포넌트는 자신의 **State**를 업데이트한다.

3.  **가상돔(Virtual DOM)**: 컴포넌트의 State나 Props가 변경되면, 리액트는 해당 컴포넌트를 **다시 렌더링(Re-rendering)**하라고 지시한다. 이때 리액트는 실제 브라우저의 DOM을 바로 건드리지 않는다. 대신 메모리에 존재하는 **가상돔(Virtual DOM)**에 먼저 변경 사항을 그린다.

4.  **재조정(Reconciliation)**: 변경된 가상돔과 이전 가상돔을 비교하여 **실제로 바뀐 부분**만 계산한다(이 과정을 'Diffing'이라고 한다).

5.  **DOM 업데이트**: 마지막으로 이 최소한의 변경 사항만 실제 브라우저 DOM에 한 번에 적용한다. 이 덕분에 불필요한 DOM 조작을 최소화하여 뛰어난 성능을 유지할 수 있다.

리액트는 기존의 UI 개발 방식이 가졌던 고질적인 문제들을 효과적으로 해결했다.

- **복잡한 DOM 관리와 '스파게티 코드' 문제**

  - **문제점**: 리액트 이전에는 jQuery 등을 사용해서 데이터가 바뀔 때마다 어떤 DOM 요소를 어떻게 바꿀지를 개발자가 일일이 명령해야 했다. 이는 UI와 기능이 복잡해질수록 데이터와 UI의 관계를 추적하기 어렵게 만들어 유지보수가 힘든 '스파게티 코드'를 뿜어냈다.

```
// 이 코드 처럼 과거에는 콜백을 통한 명령형으로 코드를 작성해야했다. 이런 경우 코드가 조금만 복잡해져도 콜백지옥이나, 데이터와 UI의 관계가 꼬여버리는 등의 복잡성이 생긴다.
<div id="count">0</div>
<button id="btn">+1</button>

<script>
  let count = 0;
  document.getElementById("btn").addEventListener("click", () => {
    count++;
    // 개발자가 직접 DOM 업데이트
    document.getElementById("count").innerText = count;
  });
</script>
```

- **해결책**: 리액트는 **선언형 프로그래밍**을 채택했다. 개발자는 "이 state에서는 UI가 이런 모습이어야 한다"고 선언만 하면, 리액트가 DOM 업데이트를 알아서 처리해 준다.

```
// 반면 리액트의 경우 이렇게 선언적으로 코드를 작성할 수 있다.
function Counter() {
  const [count, setCount] = React.useState(0);

  return (
    <div>
      <div>{count}</div>
      <button onClick={() => setCount(count + 1)}>+1</button>
    </div>
  );
}
```

- **잦은 DOM 업데이트로 인한 성능 저하**

  - **문제점**: 브라우저에서 DOM을 직접 조작하는 작업은 비용이 매우 크다. 데이터가 변경될 때마다 DOM을 업데이트하면 UI가 버벅거리거나 전체적인 성능이 저하될 수 있다.

```
function updateList(items) {
  const ul = document.getElementById("list");
  ul.innerHTML = ""; // 전체 삭제 후 다시 그림
  items.forEach(i => {
    const li = document.createElement("li");
    li.textContent = i;
    ul.appendChild(li);
  });
}
```

- **해결책**: **가상돔(Virtual DOM)과 재조정(Reconciliation)** 메커니즘을 통해 DOM 업데이트를 최소화하고 여러 변경 사항을 하나로 묶어(batching) 처리함으로써 성능을 최적화했다.

```
function ItemList({ items }) {
  return (
    <ul>
      {items.map(i => (
        <li key={i}>{i}</li>
      ))}
    </ul>
  );
}
```

- **UI의 재사용성과 모듈화 부재**
  - **문제점**: 비슷한 UI 요소(버튼, 입력창 등)가 여러 곳에서 사용될 때, 코드를 복사-붙여넣기 하는 경우가 많았다. 이는 코드 중복을 지속적으로 일으키고, 수정이 필요할 때 모든 곳을 찾아 바꿔야 하는 불편함을 만들어냈다.

```
// 비슷한 형태의 컴포넌트가 반복 될 때 코드 복붙
<button style="color:white;background:blue">저장</button>
<button style="color:white;background:blue">취소</button>
```

- **해결책**: **컴포넌트 기반 아키텍처**를 통해 UI를 독립적이고 재사용 가능한 단위로 개발할 수 있게 했다. 잘 만들어진 컴포넌트 하나는 애플리케이션의 어느 곳에서든 쉽게 재사용할 수 있어 개발 생산성과 유지보수 효율을 극대화했다.

```
function MyButton({ label }) {
  return (
    <button style={{ color: "white", background: "blue" }}>
      {label}
    </button>
  );
}
// Props랑 State를 이용한 선언적 컴포넌트로 재사용성 극대화
<MyButton label="저장" />
<MyButton label="취소" />
```

오늘 리액트에 대해 정리하면서 느낀 건데, 정말 깊이 공부해봐야할 기술이나 개념이 많다는 걸 새삼 깨달았다.
특히 Virtual DOM과 Reconciliation 부분을 공식문서를 찾아보고 정리하면서, 막연히 "그냥 빠르다"고만 알고 있었는데 실제로는 Diffing 알고리즘이나 Fiber 아키텍처 같은 복잡한 개념들이 숨어있었다. 이젠 이런 부분들을 더 깊이 파보고 싶다.
그리고 선언형 프로그래밍과 명령형 프로그래밍의 차이점을 예제로 작성하면서, 이게 단순히 문법의 차이가 아니라 사고방식 자체가 다르다는 걸 다시 한번 느꼈다. 명령형은 "어떻게(How)"에 집중하고, 선언형은 "무엇을(What)"에 집중한다는 차이점이 코드의 복잡성과 유지보수성에 이렇게까지 큰 영향을 미치는지 몰랐다.
다음엔 선언형 vs 명령형에 대해서 써봐야겠다. 함수형 프로그래밍이나 불변성(Immutability) 개념도 함께 정리하면 좋을 것 같고...
사실 그동안 너무 "쓰는 것"에만 집중하고 "왜 그런지", "어떻게 동작하는지"에 대해서는 소홀했던 것 같다. 이번 기회에 기초부터 차근차근 다시 정리해봐야겠다.
