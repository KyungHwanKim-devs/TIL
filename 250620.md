### 스택 vs 힙

프로세스의 메모리 구조는

- **스택(Stack)**: 지역 변수, 함수 매개변수, 리턴 주소 등이 저장된다. 함수가 호출되면 자동으로 할당되고, 함수가 끝나면 자동으로 해제된다. 관리가 자동이고 빠르다. 대신 크기가 제한적이다(보통 8MB).

- **힙(Heap)**: 프로그래머가 직접 관리하는 동적 메모리 영역이다. `malloc()`, `free()` 또는 `new`, `delete`로 명시적으로 할당하고 해제한다. 크기 제한이 거의 없다(가상 메모리 크기까지). 대신 관리가 복잡하고 느리다.

스택은 간단하다. 그냥 스택 포인터를 올리고 내리면 된다고 한다(사실 포인터를 아직 공부 안해봐서 직관적으로 이해되지는 않는다). 힙은 프로그램이(개발자가) 아무 때나 아무 크기의 메모리를 요청할 수 있다.

### malloc()의 내부 동작

`malloc(size)` 를 호출하면 대략 이런 일이 일어난다:

1. **힙 매니저 확인**: `malloc()`은 사용자 공간 라이브러리 함수다(glibc의 일부). 먼저 내부적으로 관리하는 힙 영역에 요청한 크기만큼의 빈 공간이 있는지 확인한다.

2. **적절한 블록 찾기**: 힙은 여러 크기의 블록들로 나뉘어져 있다. 힙 매니저는 다양한 알고리즘(First Fit, Best Fit, Worst Fit 등)으로 적절한 블록을 찾는다.
   - **First Fit**: 처음으로 크기가 맞는 블록을 준다. 빠르지만 단편화가 심할 수 있다.
   - **Best Fit**: 딱 맞거나 가장 낭비가 적은 블록을 준다. 느리지만 메모리 효율이 좋다.
3. **블록 분할**: 찾은 블록이 요청한 크기보다 훨씬 크면? 블록을 둘로 쪼개서 필요한 만큼만 주고 나머지는 다시 Free List에 넣는다.

4. **메타데이터 관리**: 할당한 블록의 앞부분에 크기 정보, 할당 여부 등의 메타데이터를 저장한다. `free()`할 때 이 정보를 보고 얼마나 해제할지 안다.

5. **메모리 부족 시**: 힙에 충분한 공간이 없으면? 커널에게 더 요청한다. `brk()` 또는 `mmap()` 시스템 콜을 사용해서 힙 영역을 확장한다(중요 !!).

### 메모리 단편화 문제

힙을 계속 쓰다 보면 **Fragmentation** 문제가 생긴다.

메모리를 불규칙한 크기로 계속 할당, 해제를 반복하다보면 빈 공간이 충분히 있더라도 연속되어 있지 않아서 할당할 수 없는 경우가 생긴다. 이게 바로 **External Fragmentation**다.

이런 외부단편화들은 다음의 방법으로 해결한다고 한다

- **메모리 통합(Coalescing)**: 인접한 빈 블록들을 합친다. `free()` 할 때 양 옆을 확인해서 빈 블록이면 하나로 합친다.
- **메모리 압축(Compaction)**: 사용중인 블록들을 한쪽으로 몰아서 빈 공간을 모은다.
- **풀 할당자(Pool Allocator)**: 같은 크기의 객체만 할당하는 전용 풀을 만든다.

### 메모리 누수

힙은 `malloc()`으로 할당한 메모리를 `free()`로 해제하지 않으면 그 메모리는 영원히 다시 사용할 수 없다. 프로그램이 실행되는 동안 계속 메모리를 먹다가 결국 Out Of Memory로 프로세스가 죽을수 있다.

사실 매모리 할당(malloc(), new, brk(), mmap())은 책과 여러 자료를 찾아보고 학습해 봤는데 분량이 너무 많아서 일단 이렇게 전반적으로 먼저 정리해봤다. 아마 추후에 더 깊은 이해를 위해 각 요소를 따로 몇편씩 작성해야할것 같다.
